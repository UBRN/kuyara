# Current Project Status

## Current State

- **Mobile:** Expo SDK 57, React Native, Expo Router, and Expo SQLite provide an accountless five-step onboarding flow ending with optional location selection; three primary tabs, Today, Weather, and Profile, drawn by Expo Router Native Tabs so the OS renders Liquid Glass and its own accessibility adaptation, with Wardrobe and Settings as Profile stack destinations; private Wardrobe photos; Turkish/English localization; System/Light/Dark appearance; and semantic haptics at the six sites the design language names. The minimum supported iOS is 26.0.
- **Weather:** Mobile preserves the last valid snapshot, refreshes data older than 30 minutes, and reaches providers only through the Worker. The chain is WeatherKit, Open-Meteo, then OpenWeather, with bounded attempts, runtime validation, attribution, rate limiting, and a best-effort OpenWeather daily cap. The deterministic provider is test-only.
- **Recommendations:** The deterministic layer composes at most 24 complete, valid, requirement-satisfying, formality-consistent outfits from the bundled catalog. The AI selects exactly three of them and labels each with one archetype identifier from a closed twelve-entry list; the Worker uses Workers AI followed by OpenRouter and validates option membership, count, distinctness, and archetype preconditions. Mobile persists the result and falls back to a device-local deterministic generator that assigns archetypes by the same rules. Generation triggers by comparing current signals against the persisted recommendation snapshot's signals, not an in-memory baseline. Today and outfit detail show the archetype name as the outfit title. Today and Settings expose only the coarse generation mode, and Settings includes the bounded active AI probe.
- **Notifications:** N1 is complete. Schema v6 stores `notifications_opt_in`; Settings owns the permission flow and OS-settings redirect; notification taps open Today; and the test action is development-only. N2 weather-alert rules and background rescheduling are not implemented.
- **Builds:** iOS is the first release target. EAS production credentials, a production build, and an App Store Connect record (`com.ubrn.kuyara`, ASC app `6806664440`) exist; build 1.0.0 (2) is on TestFlight internal testing and was verified on a physical device against the deployed Worker. EAS preview and production profiles use the deployed Worker; development keeps its local Worker fallback. Shared code remains Android-compatible, but Android validation is deferred.

The shipped application still has no account, cross-device sync, analytics, or server-sent push. WeatherKit is configured and serving live traffic at the head of the provider chain.

Three durable directions were settled on 2026-09-04 and are documentation-only; none has
any implementation. kuyara is no longer described as fundamentally local-first, and
Supabase Auth, PostgreSQL and Storage are the intended long-term backend, with Postgres
authoritative for account-backed data once accounts exist and SQLite remaining the
device-side working store ([ADR 0022](adr/0022-supabase-is-the-intended-backend-and-kuyara-is-not-local-first.md)).
Behavioural product analytics is approved with PostHog as the provider and is sequenced
before the first public release ([ADR 0023](adr/0023-behavioural-product-analytics-with-posthog.md)).
The project moved from MIT to the PolyForm Noncommercial License 1.0.0 and is
source-available rather than open source ([ADR 0024](adr/0024-relicensing-to-polyform-noncommercial.md)).

## Recently Completed

- **Local weather alerts scheduled from the planner** (2026-09-09): [ADR 0032](adr/0032-local-weather-alert-rules.md) phase 2. The notification gateway grew a cancel-all for kuyara's own `weather-alert:` identifiers and a date-trigger schedule with an Android `weather-alerts` channel; schema v11 adds the `weather_alert_deliveries` ledger; a `WeatherAlertScheduler` cancels, deletes the profile's still-pending ledger rows, plans with the device time zone's quiet hours, schedules localized copy in the active language, records the plans and prunes rows older than three days, coalescing concurrent calls; a `WeatherAlertObserver` mounted inside the weather provider reschedules on snapshot id, opt-in, permission and language changes, and passes a stale snapshot through rather than cancelling, so a failed refresh keeps the last plans. Two defects found in review were fixed in the same Codex lane: cancelled plans poisoned the ledger, and staleness cancelled every alert. Verified on the iPhone 17 Pro Simulator: the dev database migrated to v11, opt-in through the Settings toggle and the system prompt, and a rain hour seeded into the persisted forecast produced one ledger row and, at its fire time, the localized banner. Tests: 366 Node, 196 component, `pnpm check` green. The Android channel is unverified, consistent with the repository's Android posture.

- **Local weather alert rules decided, and the planner built** (2026-09-09): [ADR 0032](adr/0032-local-weather-alert-rules.md) closes the threshold question ADR 0004 left open for N2. Two rules over the remainder of the current local day, precipitation onset and an 8 °C apparent-temperature swing, 60 minutes ahead, silent from 22:00 to 07:00 in the device time zone, one alert per rule per location per day, and structured plans rather than copy. The numbers are the requirement engine's own lines, and a test pins them. Phase 1 landed as `features/notifications/domain/weather-alerts.ts` with sixteen boundary tests that pass under `TZ=UTC`, `Europe/Istanbul` and `Pacific/Kiritimati`; nothing imports the module yet, so the shipped app is unchanged. The Codex lane that wrote it ran out of usage before its checks, which the orchestrator ran instead; a separate read-only Codex review found no defects and probed DST gaps in New York and Lord Howe. Hermes `Intl` behaviour on the device is verified with phase 2. Phases 2 (scheduling, ledger at schema v11, the observer) and 3 (`expo-background-task`, verified on SDK 57 as 57.0.16) are next.

- **Apple's privacy obligations for analytics, verified** (2026-09-09): milestone 9. [ADR 0033](adr/0033-apple-privacy-obligations-for-first-party-analytics.md) reads the current App Store Review Guidelines and App Privacy documentation against ADR 0023 and settles the open consent question: guideline 5.1.1 (ii) requires consent before collection and an in-app way to withdraw it even for anonymous usage data, so ADR 0023's "no permanent toggle" preference is withdrawn; the withdrawal control is unprominent but two taps deep, on a Privacy surface under Settings with the in-app policy link. ATT does not apply, with Apple's definition cited. Analytics enters the questionnaire as Product Interaction and Other Usage Data, with PostHog's IP capture off so no Coarse Location category appears. Six open questions for the maintainer, including whether the per-install identifier counts as linked, stay in the ADR.

- **The analytics event taxonomy, drafted** (2026-09-09): milestone 8. [`docs/analytics-taxonomy.md`](analytics-taxonomy.md) is the reviewable tracking plan ADR 0023 asked for: twenty custom events across the ADR's areas, each with a stable `snake_case` name, trigger, typed low-cardinality properties, the product question it answers and its sampling rule, plus a `schema_version` on every event, the payload exclusion list as a reviewer checklist, and the identity decision: the SDK's own random per-install identifier, never derived from or sent beside `localProfileId`. Gender is absent from every event; the coarse age bucket and dress style are the only profile-derived properties. Awaiting the maintainer's review; it installs nothing.

- **Goal 7, the accessibility and polish validation pass** (2026-09-09): the native half of design goal 7, run over the shipped app rather than a disposable spike, because every redesigned surface is implemented. Method, so the run can be repeated: the dev build on the iPhone 17 Pro Simulator with Metro on 8081; captures by `xcrun simctl io <udid> screenshot` and by Maestro 2.8.0 `takeScreenshot` inside four tour flows (Java from the Homebrew OpenJDK, the same path `pnpm e2e:ios` uses); screen-reader semantics and touch targets from `maestro hierarchy`, which dumps the XCUITest accessibility tree with labels, traits and frames; the largest text size through `xcrun simctl ui <udid> content_size accessibility-extra-extra-extra-large` (fontScale 3.118); dark and language through the app's own Appearance and Language pickers; Reduced Motion through `xcrun simctl spawn <udid> defaults write com.apple.Accessibility ReduceMotionEnabled -bool true` followed by an app relaunch, proven by diffing two captures 1.5 s apart (0 of 3,162,132 pixels differ with the setting on, 724 with it off, all inside the Weather cloud glyph); contrast from the theme tokens (WCAG arithmetic, reproduced below); onboarding by resetting `onboarding_completed` in the development database and relaunching. Not run: Accessibility Inspector (desktop control was declined in this session) and real VoiceOver (no device). Passes: Turkish light default size (all surfaces), Turkish dark largest size (all surfaces, plus onboarding), English light largest size (all surfaces except AI status and the pickers, which the tour did not reach), English light default size for onboarding. Captures live beside the convergence sheet in the vault, `kuyara/target-set/goal-7-captures/`, named `<screen>-<axis>-<lang>-<appearance>.png`, with the hierarchy dumps as `.json` next to them; nothing was added to the repository. One environment incident is recorded because it changed the evidence base: between 14:38 and 14:45 the iOS 26.3 device used for the first two passes was deleted and a new iPhone 17 Pro on iOS 26.5 was created with a rebuilt `kuyara.app` while other interactive Claude sessions were active on this machine; this session ran no delete, erase, build or install. The development database on the old device (Turkish profile, one Closet item, device location, cached recommendation) went with it, so the English pass and the Reduced Motion check ran on iOS 26.5 with a fresh profile (man, smart, empty Closet, a simulated location set with `xcrun simctl location set`). The two answers goal 7 owed: the spring role is **not measurable until ADR 0026 decision 7 lands**, because no `Animated`, Reanimated, `LayoutAnimation` or spring code exists in Today, outfit detail or the garment board; the only transition between the two screens is the platform push, which the root layout already sets to `none` under Reduced Motion, and the garment entrance motion does not exist either, so the shipped behaviour is decision 7's Reduced Motion path for everyone. The 2026-09-07 `ListItem` leading-tile answer **still holds on shipped Settings**: `native-list.tsx` hosts the shared `ListRowTile` in the `leading` slot through `RNHostView matchContents`, the captures show it at 28 points at the default size and the hierarchy shows it at 42 by 42 at the largest size, so the tile scales through the shared `min(fontScale, 1.5)` cap while the rest of the native row scales without a cap; whether ADR 0030 section 3's "the cap does not apply here" meant the tile too is a wording question left to the native-list follow-up below. Also closed here: the tab bar touch target ADR 0027 said the repository could not measure is 94 by 54 points per item in the hierarchy, and the labels stay at their normal size at the largest text size in every capture, as ADR 0027 section 5 recorded. What breaks first, in one sentence: the surfaces that put a kuyara-drawn label beside a control or another label on one row break by character at the largest size in both languages, and the outfit detail's in-place captions are the worst case because they overlap each other.

  **Confirmed defects.** Capture paths are relative to the vault folder named above.

  | Severity | Screen | Axis | What breaks | Capture | Proposed follow-up |
  | --- | --- | --- | --- | --- | --- |
  | High | Outfit detail | Largest text, both languages | The captions under each detail piece break by character and overlap each other and the shoe drawing; the hierarchy shows the light-jacket box [244,470][355,927] intersecting the shoes box [222,629][378,1030]. Illegible. | `outfit-detail-top-tr-dark-ax5.png`, `outfit-detail-top-en-light-ax5.png`, `outfit-detail-screenreader-tr-dark-ax5.json` | Design decision plus lane: a caption layout for `fontScale` above 1.5 (captions listed under the board, or a wider caption column) added to ADR 0026 decision 3 |
  | High | Weather | Largest text, both languages | The location card's label breaks by character ("Me vc ut", "Cu rre nt") beside the Change control, and the card grows to 692 points, taller than the viewport, so Maestro could not scroll it fully into view. | `weather-top-tr-dark-ax5.png`, `weather-top-en-light-ax5.png`, `weather-screenreader-tr-dark-ax5.json` | Lane: stack the change control under the label above 1.5, the rule `ListRow` already applies |
  | Medium | Settings root | Largest text, both languages | Native rows keep label and value on one line, so "Görünüm", "Bildirimler" and "Ayarlanmadı" break by character in Turkish and "Lan-guag e", "Ap-pear-ance" and "Sys-tem" hyphenate in English; ADR 0030 section 3 says the value stacks under the label above 1.5, and shipped rows do not. | `settings-top-tr-dark-ax5.png`, `settings-bottom-tr-dark-ax5.png`, `settings-top-en-light-ax5.png` | Lane: `NativeListRow` stacks the value above 1.5, or the ADR records what the SwiftUI `ListItem` allows |
  | Medium | AI status | Largest text | The tinted row label "AI durumunu kontrol et" does not scale with Dynamic Type while everything around it is three times larger. | `settings-ai-status-tr-dark-ax5.png` | Lane: the tinted and secondary label path in `native-list.tsx` takes a scaling text style |
  | Medium | Closet | Largest text | The category chips are clipped by their fixed 40 point height ("Tümü" cut at the top, "Tek parç" truncated); the convergence sheet assumed chips grow with their text. | `closet-tr-dark-ax5.png`, `closet-screenreader-tr-dark-ax5.json` | Lane: chip height follows the label with 40 as the minimum |
  | Medium | Settings, Birth date; onboarding step 4 | Language | The SwiftUI date picker renders the device locale, "9 Sep 2026" inside a Turkish app, and shows today's date while the value is unset next to "Ayarlanmadı" / "Not set". | `settings-birth-date-tr-light.png`, `settings-birth-date-tr-dark-ax5.png`, `onboarding-4-birth-date-bottom-tr-dark-ax5.png` | Design decision (which locale the picker follows and what an unset date shows), then a lane on `native-date-picker.tsx` |
  | Medium | Today | Safe areas | The last content ends 110 points above the tab bar (681 against 791 in the hierarchy) where ADR 0027 section 4 intends `safeAreaInsets.bottom + 12`; the automatic content inset and `Screen`'s own padding both count the bar. | `today-bottom-tr-light.png`, `today-bottom-screenreader-tr-light.json` | Lane: measure `safeAreaInsets.bottom` under native tabs and remove the double count in `Screen`, with a test on the gap |
  | Low | Outfit detail | Largest text | The emphasis pill is pushed off the right edge of the title row in both languages. | `outfit-detail-top-tr-dark-ax5.png`, `outfit-detail-top-en-light-ax5.png` | Lane: the pill wraps under the title above 1.5 |
  | Low | Outfit detail | Largest text, Turkish | The owned/wanted button pairs break mid-word ("Bend e var", "İstiyo rum"); English wraps at word boundaries. | `outfit-detail-mid-tr-dark-ax5.png`, `outfit-detail-mid-en-light-ax5.png` | Absorbed by ADR 0026 decision 5's ownership control; interim: stack the pair above 1.5 |
  | Low | Profile | Largest text, Turkish | The Closet heading "Gardırop" breaks by character beside the count and chevron. | `profile-tr-dark-ax5.png` | Lane: the count wraps under the heading above 1.5 |
  | Low | Preference pickers | Largest text, Turkish | The custom header title "Görünüm" breaks by character beside the back button; "Giyim tarzı" and "Giyim türü" wrap at the word. | `settings-appearance-tr-dark-ax5.png`, `garment-type-picker-tr-dark-ax5.png` | Lane: the shared picker header puts the title under the back button above 1.5 |
  | Low | Wardrobe form | Largest text, Turkish | The colour option "Kahverengi" breaks by character beside its radio mark. | `wardrobe-form-bottom-tr-dark-ax5.png` | Same lane as the picker header: option rows reserve the mark's width and let the label wrap |
  | Low | Today | Polish | The two alternate boards have different heights, so their captions sit on different baselines. | `today-bottom-tr-light.png` | Lane: alternates share the taller board height |
  | Low | Onboarding | Largest text, Turkish | Titles break by character where one word exceeds the column ("kuyara’y a hoş", "Cinsiyeti niz", "Konumu nu ayarla"), the step 4 date label truncates to "D…", and the pinned action bar takes about 30% of the viewport. | `onboarding-1-welcome-tr-dark-ax5.png`, `onboarding-2-gender-tr-dark-ax5.png`, `onboarding-4-birth-date-bottom-tr-dark-ax5.png`, `onboarding-5-location-tr-dark-ax5.png` | Design decision: accept single-word breaks (ADR 0028 section 3) or shorten the titles |

  **Risks, not defects.** Today has had no accessible refresh control since the header buttons left on 2026-09-08; the hierarchy shows only pull-to-refresh, so a screen-reader user refreshes through Weather's button (design decision). The colour-family silhouette fills measure 1.18 to 4.85:1 against the tile ground in light and mostly under 3:1 in dark, while the 1.9 point stroke measures 11.9:1 and 12.4:1; the rail at 136 points and the grid tile at 174 points read clearly in the captures, and the fill is content colour by ADR 0029, so this is recorded rather than filed. Several accessibility names are joined from parts (`${item}, ${slot}, ${state}` on detail captions, `${label}, ${value}` as `ListRow`'s default, the weather recap's join); they read acceptably in both languages and are lists rather than sentences. `StretchyHeader` is built and tested, has no shipped caller, and has no Reduced Motion gate. The detail captions speak the untracked state with no drawn word, the quiet default decided on 2026-09-09. The native segmented control paints 31 points tall inside its 44 point host, the system's own geometry. Text on the stage measures 4.04:1 (`textSecondary`) and 10.12:1 (`textPrimary`) in light, 5.78:1 and 13.42:1 in dark; every status ink sits within 0.7 of `brandAccent`; `borderSubtle` is 1.41:1 and 1.48:1, `borderDefined` 4.24:1 and 4.04:1, the tile fill 1.16:1 and 1.38:1 (a fill step, expected under 3:1).

  **Not run, and why.** Accessibility Inspector: desktop control was declined, so the XCUITest hierarchy stands in. Real VoiceOver: no device; spoken grouping, focus order and rotor behaviour remain unverified and stay in Known Issues. Horizontal scrolling on Weather: not testable, the shipped screen still draws the vertical hourly table because ADR 0021 section 9's rail is not built. Progressive disclosure on outfit detail: nothing on that screen expands or collapses; the wardrobe form's Details toggle is the app's only disclosure control, carries `accessibilityState.expanded`, and was exercised in every pass. The spring role: see the decision 7 answer above. Colour-only signals: no status on the toured screens relies on colour alone; the Closet chips and the pickers carry the selected trait, and every status band has ink, glyph and text.

  **Proposed follow-up order.** 1. Outfit detail at accessibility sizes (captions, emphasis pill, ownership pairs), a design decision on the caption rule followed by one lane. 2. The label-beside-a-control rule as one lane over Weather's location card, Profile's Closet heading, the picker header and the option rows. 3. Native list rows under Dynamic Type (value stacking, the tinted label, and the ADR 0030 section 3 wording about the tile cap). 4. The Closet chip height. 5. The birth date picker's locale and unset presentation, a decision before the lane. 6. The `Screen` bottom inset double count, with its test. 7. Today polish: equal alternate heights, and a decision on an accessible refresh action. 8. Onboarding titles at accessibility sizes, a decision. 9. A real-device VoiceOver pass and an Accessibility Inspector audit when a device and desktop control are available. 10. ADR 0026 decision 7 and the spring measurement, once the transition exists.

- **The silhouette rung and colour-family fill on Profile and the Closet** (2026-09-09): [ADR 0028](adr/0028-the-profile-tab-and-the-list-row-anatomy.md) section 6 and [ADR 0029](adr/0029-the-closet-grid.md) section 5 are implemented. Both personal-piece surfaces now use one `GarmentTileArtwork` ladder: a cover-cropped photo, then the approved garment-type silhouette in the piece's appearance-specific colour family, then the existing structural-category glyph for accessories and legacy null-type rows. An unreadable photo takes the same fallback. Drawn bounds fit uniformly into the centred 60% × 61% box and follow tile scaling; absent colour uses `stage`, and multicolor uses a unique two-stop blue-to-yellow gradient per tile. The tile stroke is the board's 1.9 divided by the tile's uniform scale rather than `non-scaling-stroke`, because react-native-svg 15.15.4 on iOS paints a non-scaling-stroke path in client space while a gradient fill still uses the path's local bounds, which left the multicolor fill outside the drawing on the Simulator; the board is unaffected because it fills with a solid colour. The fill table is content colour for these two surfaces only. Tile geometry, copy and accessibility labels are unchanged. Contrast assertions and component coverage cover both appearances and fallback states; native Simulator acceptance remains with the orchestrator. The five accessory drawings remain unapproved.

- **The recommendation detail surface** (2026-09-09): [ADR 0026](adr/0026-the-recommendation-detail-surface.md) decisions 1 to 6 are implemented. `GarmentBoard` now exposes additive pixel layout geometry and a decorative mode, so detail reuses the unchanged composition algorithm and detail preset while each absolutely positioned caption owns its piece name, slot and truthful owned/wanted state semantics. Caption measurements extend the plate before reasoning begins. Requirement evaluations are joined to localized catalogue names in presentation and rendered one row per met requirement or trade-off; the existing ownership actions remain available without changing their behavior, and the foot of the screen carries the existing temperature, condition and rain probability on the stage tint. English reuses the existing Owned/Wanted strings; Turkish adds “Sende var” / “İstiyorsun”. No schema, contract, Worker, dependency, substitution affordance or data fetch changed. The domain's third `none` ownership state has no approved caption-state copy, so untracked pieces deliberately show no state marker or word while keeping both actions. Decision 7's entry transition remains open, and Simulator verification belongs to the orchestrator.

- **Today's garment board, and the Direction E tokens under it** (2026-09-08): [ADR 0025](adr/0025-the-garment-board-composition-rule.md) is implemented. `components/ui/garment-board` carries the 22 approved silhouettes and the six redrawn category glyphs as path data with authoring-time drawn bounds (a test re-derives the bounds from the paths), the composition rule ported from the reference implementation with the Today and detail presets, and one `GarmentBoard` primitive on `react-native-svg`, the only importer of that package. The ten evidence slot lists pass overlap 0, clipping 0 and anchor parity 1.000 under both presets. Today now renders the approved target-set layout: a place row, the `stage`-filled board with the temperature and condition in its corner, the archetype at `title`, one rationale, a provenance line that shows the AI-assisted mark only when AI contributed, and two full alternate boards (ADR 0021 section 6 amended the same day). The header buttons went with the stretchy header: Settings is reached from Profile, refresh is pull-to-refresh. The theme moved to the Direction E allocation at the same time, because the stage is invisible on the old ground: light `background` is Soft Mist, `textSecondary` and `iconSecondary` are a derived neutral, `borderSubtle` is the mockup hairline, and `stage` is a new role; `theme.test.mjs` retired the 1.2:1 light card step per ADR 0021's amendment and gained the stage floors. Deviations from the mockup, each with a reason: the archetype and temperature use `title` and the small lines `caption` because the mockup's 30, 28, 12 and 11.5 point sizes are not ADR 0017 roles; the alternates heading is sentence-case `bodyStrong` rather than an uppercase eyebrow (Law 1); above font scale 1.5 the temperature row sits above the stage so scaled text cannot collide with width-sized garments. Still open: the entrance motion and the Today-to-detail transition (design goal 7 measures the spring), the per-condition stage tint (ADR 0018's seven states at ADR 0021's raised luminance have no recorded values), and the silhouette rung in the Profile rail and Closet grid, which can now reuse the primitive.

- **Location in onboarding, and an honest empty state** (2026-09-08): [ADR 0016](adr/0016-location-in-onboarding-and-an-honest-empty-state.md) is implemented. Onboarding now ends with an optional fifth step that explains why location is useful, offers the existing foreground device-location flow, embeds the same debounced live city search and results used by `/weather/location`, and stays completable through a quiet final action that reads Not now until a location exists and Start using kuyara once one does. The step sits inside its own top safe-area view because it renders the shared controls rather than the scrolling `Screen`. The existing rationale card is shared unchanged after the device action, so no second permission path or controller exists. Today distinguishes a null active location from genuine unavailable failures, names the cause, and links to `/weather/location`; generic failure copy remains unchanged. The weather provider was already mounted above onboarding, and no persistence, migration, Worker, contract, dependency or route-gate change was needed. Automated verification is recorded in the delivery report; Simulator verification remains with the orchestrator.

- **The location picker screen, implementation** (2026-09-08): Weather's existing Change control now opens `/weather/location` in its tab stack. The native control surface uses the existing `NativeList` wrappers and one new `NativeTextField`, with the device-location and permission paths moved from Weather. Search trims and debounces for 300 ms, requests five places in the resolved language, ignores superseded responses, drops results with no time zone, and shows localized loading/empty/error states and Open-Meteo/GeoNames attribution. Selection uses the existing weather controller persistence/reset/refresh path; Weather reads the persisted display name for both searched and legacy locations. Profile's route and Today's presenter read the same persisted display name, and the sample-city and inline-picker copy was deleted with its last reader. The sample catalog and `selectManualLocation` remain only as test and persisted-row compatibility. Verified on the iPhone 17 Pro / iOS 26.3 Simulator against the live Worker: typing "Ista" returned five places with the Open-Meteo/GeoNames footer, selecting Istanbul showed the trailing Selected value, Weather, Profile and Today all showed "Istanbul", and the selection survived a kill and relaunch. No migration, dependency, Worker or deployment changes.

- **Dress style replaces the age band** (2026-09-08): [ADR 0031](adr/0031-dress-style-is-the-formality-signal.md) is implemented across the strict shared contract, Worker prompt and cache, mobile profile and schema v10 persistence, recommendation selection and cache comparison, four-step onboarding, Settings, localization, tests, and Maestro flows. Required `dressStyle` is `casual`, `smart`, or `formal`; it reorders the same candidate set and excludes nothing. Existing profiles return to onboarding once, while persisted recommendation contexts written with the retired field or with neither style field read as `smart`. Birth date remains optional and device-only, drives no product logic, and changes no recommendation. Accepted after two corrections outside the lane: the legacy-context guard in `worker-ai-recommendation-mapper.ts` had assembled the retired key from fragments to satisfy the brief's grep invariant and now names `ageBand` plainly with the reason it exists (development databases hold band contexts, and a strict parse failure would take the whole persisted recommendation down), and one garbled sentence in ADR 0015's header. Verified on the iPhone 17 Pro / iOS 26.3 Simulator against a version 9 database: migration 10 added `dress_style` and reopened onboarding with the gender prefilled; the four steps rendered; continuing past dress style without a choice showed the required error; Smart, then completion with no birth date persisted `woman | smart | null | 1`; Settings' About you showed Gender "Woman", Dress style "Smart" with the new `clothing` glyph and Birth date "Not set", and the dress style picker opened with Smart selected. `pnpm check` and `pnpm --filter @kuyara/mobile test:components` (160/160) passed; `pnpm e2e:ios` passed 2/2 (Complete fresh-install onboarding 12s, Update and persist settings 52s). One development-only hazard recorded here rather than in Known Issues: a database that already ran the earlier, narrower version 10 lacks `dress_style` and fails to start; no shipped build wrote version 10, so only development Simulators can be in that state, and resetting `PRAGMA user_version` to 9 or clearing app data recovers them.

- **Gender and birth date in onboarding and Settings** (2026-09-08): ADR 0015 phase 3 landed. Onboarding now asks for required gender and optional birth date, keeps language and appearance on their existing system or Settings choices, and prefills both personal fields when schema v10 returns existing installations to onboarding once. Settings replaces clothing preference with Gender and Birth date rows, uses the system date picker through the control-layer wrapper, formats the stored date for the active language, and supports clearing it back to null. The completion write leaves language and theme untouched; migration tests preserve every other profile field, dependent row, weather snapshot and recommendation snapshot across the reset. Focused reducer, persistence, migration and component tests cover validation, null completion, prefill, row values, picker order and clearing. Verified with `pnpm check` and `pnpm --filter @kuyara/mobile test:components` (160/160 across 19 suites). Ponytail review is recorded in the delivery report. Accepted after three corrections made outside the lane: `NativeDatePicker` hosts itself when it is not inside `NativeList` (the SwiftUI view mounted in a bare UIView in onboarding and raised the red box; the standalone `Host` takes an explicit height because `Host matchContents` collapses inside a ScrollView, the ADR 0019 mount-check finding), `iconNames` gained a `calendar` entry for the Birth date row, and the birth date surface states "Not set" as its footer because a compact system date picker always displays a date, today when nothing is stored. Verified on the iPhone 17 Pro / iOS 26.3 Simulator against the existing version 9 database: migration 10 reopened onboarding with the gender prefilled, the birth date step picked, cleared and completed with null, Today kept its location and weather, Settings showed Gender "Woman" and Birth date "Not set", the surface saved "1 Sep 2026" and offered removal, and the root row then read "September 1, 2026". `pnpm e2e:ios` passed 2/2 (Complete fresh-install onboarding 13s, Update and persist settings 52s). TestFlight users will see onboarding once after installing this build. Historical note: ADR 0031 superseded this completed slice by inserting required dress style between gender and birth date and making birth date inert.

- **Real location selection, below the UI** (2026-09-08): `packages/contracts/src/place-search-v1.ts` defines strict request, result and minimal error schemas for `POST /v1/places/search`: 2 to 100 query characters, 1 to 5 results, Turkish or English, stable `place.<positive decimal id>` ids, display name, region/country line, hundredth-degree coordinates and a nullable IANA time zone. Open-Meteo Geocoding is the orchestrator-approved provider assumption, not a previously recorded provider decision; `apps/worker/src/places/` isolates raw Zod validation, explicit mapping, one upstream call with a four-second timeout, sanitized failures and controlled `open-meteo` / `geonames` attribution identifiers. The route shares the existing weather per-IP rate-limit budget and fails closed if the binding is absent or fails; no queries or coordinates are logged by the new code. `apps/mobile/src/features/weather/data/worker-place-search-data-source.ts` is the future picker's Worker-only search entry point. `ManualLocationId` now accepts validated `sample.<slug>` and `place.<positive decimal id>` shapes, replacing the duplicate repository whitelist; schema v9 in `apps/mobile/src/infrastructure/sqlite/migrations.ts` persists manual display names and backfills all three sample cities while preserving device selections and cached data. Focused tests cover search success/empty/error/timeout/rate limits, input and output boundaries, persistence round trips, migration backfill, rollback and retry. `pnpm check` passed (contracts 34/34, Worker 148/148, mobile Node 274/274, lint, TypeScript and Worker dry-run bundle); `pnpm --filter @kuyara/mobile test:components` passed 157/157 across 18 suites; `git diff --check` passed. Ponytail review found no unnecessary complexity. No UI, dependency, ADR, deployment or secret changes; live provider behavior and current provider terms were not checked because this lane forbids network access. The first live check, after the 2026-09-08 deployment, found the route returning 503 on every query: two workerd-only defects the injected-fetch tests could not see. `OpenMeteoPlaceProvider` stored `globalThis.fetch` on the instance and called it as `this.fetch`, which workerd rejects with "Illegal invocation" while Node tolerates it, and the request used `redirect: 'error'`, which Workers' fetch does not accept. Fixed by binding the global and using `redirect: 'manual'` (a redirect then fails the existing `response.ok` check), reproduced and confirmed against `wrangler dev`, redeployed, and verified live with a Turkish query returning three places with Open-Meteo and GeoNames attribution. The general lesson is recorded here rather than in Known Issues: a Worker adapter is not verified until it has run under workerd, because Node's fetch accepts what workerd rejects. Verified on the iPhone 17 Pro / iOS 26.3 Simulator against the existing database: the app relaunched through migration 9, the sample picker still lists its three cities, and selecting Sample Istanbul persisted the manual location with its display name and fetched weather.

- **Gender and age band, below the UI** (2026-09-08): ADR 0015 phases 1, 2, 4 and 5 landed. `infrastructure/sqlite/migrations.ts` rebuilds the profile at version 8, converts both catalog preferences to gender, adds nullable ISO `birth_date`, and preserves every other column, onboarding completion and dependent rows. Foreign keys are deferred and checked on the transaction connection before commit; a failed rebuild rolls back. The ADR names static year bounds without numbers, so implementation uses 1900 to 2100; the domain separately rejects invalid calendar dates and future dates. Profile domain, record, repository and application code store gender and birth date, while one controller mapping derives the existing `clothingPreference` and translates legacy picker writes back to gender. `packages/contracts/src/ai-v1.ts` shares the three bands and formality order; `ageBand` is optional in the request per ADR section 6, with absence treated as `adult` for older clients, while mobile always sends its derived band. Recommendation application/data code includes the band in persisted cache comparison and request coalescing; a within-band date correction preserves the snapshot. Fallback ranks the same option set and selects three distinct valid archetypes. Worker prompt and shared cache include the band, never birth date or birth year; response and presentation files are unchanged. Tests cover version 7 upgrades, dependent rows, rollback, profile round trips, date and band boundaries, all three formality orders, fallback across both catalogs and seven day variants, privacy, prompt content and cache separation. Verified with `pnpm check` (contracts 30/30, Worker 141/141, mobile Node 263/263, lint, typecheck and bundle), `pnpm --filter @kuyara/mobile test:components` (157/157), and `git diff --check`. The bundle was also rerun with Wrangler diagnostic logs directed to `/tmp` after the sandbox denied its default log path. Ponytail findings were simplified and resolved. Phase 3, including onboarding reset and the new onboarding/Settings rows, remains; Simulator verification of migration on a real database belongs to the orchestrator. Reviewed read-only before commit: the Codex astra reviewer ran out of usage, so an Opus reviewer took the same brief and found one blocker, that the moving future bound on the birth date was applied on the read path so a device with a wrong clock would fail profile mapping at launch and hold the app on the bootstrap screen; the read path now checks only the static shape and year bounds the SQLite CHECK enforces, the moving bound stays on the write, and a persistence test relaunches with a 1970 clock against a stored date. Two should-fixes followed: the AI prompt no longer receives `ageBand` beside the `formalityOrder` derived from it, only the order, and the Worker-before-mobile deployment order is recorded under the milestone. Three nits were taken (an unreachable `ageBand` on the legacy context schema, two dead defaults); the fourth, `composeOutfits` keeping only test callers after `composeOutfitOptions` took over, is recorded in Known Issues. Verified on the iPhone 17 Pro / iOS 26.3 Simulator against the existing version 7 database: the app relaunched through migration 8 with its language, appearance and clothing preference intact and Settings rendering as before. Historical note: ADR 0031 superseded the age band with required dress style before release.

- **The 2026-09-07 UI programme, independently reviewed** (2026-09-07): the ten commits from the bottom-inset fix to the small known-issue fixes were reviewed read-only against the code review rules, the design laws and the ADRs. The Codex astra reviewer the orchestration policy prefers ran out of usage before reporting, so the review was run by an Opus reviewer with the same brief; a Codex re-review is still worth running once that limit resets. Nine findings, all verified against the source before acting. Fixed in one commit: the Closet grid's tile width was the ADR's 393 point reference number hard-coded, which clips the right column on a 375 point device, so `resolveGridGeometry` now derives the tile from the window width and reproduces 174.5 by 218 and 361 by 280 at 393 points, with a test at 393 and 375; Profile's rail-to-group gap compounded the column gap and the group margin to 36 where ADR 0028 says 24; `NativeList` applied no bottom safe-area inset on the three Settings surfaces, and the design-language check that greps for `paddingBottom` covers only `Screen`, so the inset rule is now restated for any screen-level scroll container; the Closet heading's accessibility label repeated its hint's action; `IconButton` gained a `quiet` variant so two route files stop un-styling it inline; the outfit detail helper's formatting; and the list-row test suite gained the `fontScale` 1.5 case, the tightest one in ADR 0028 section 3. Recorded rather than fixed: the community segmented control ignores `tintColor` on iOS (now a Known Issues bullet), and ten `wardrobe.*` localization keys have no caller after the grid landed. On 2026-09-08, 15 unused localization keys were removed, along with the retired test-notification path that served five of them. Verified with `pnpm --filter @kuyara/mobile test:components` (157/157) and `pnpm check`.

- **Goals 1 to 3, audited against the list-row checklist** (2026-09-07): the documented pass the checklist's adoption owed, run over the composition-rule, recommendation-detail and app-shell material outside the repository and over ADR 0025, 0026 and 0027; no application code changed. Checks 1 to 5 (grouped rows, the leading tile, the separator, the trailing value, section headings) do not apply: none of the three surfaces has a list row, and ADR 0026 organises reasoning by weather requirement in captioned rows of its own, which ADR 0029 section 6 already allows for a later addition. Check 6 (one accent fill per viewport) holds on Today's board and on the detail surface as drawn; the known tension is the shipped outfit detail's per-slot ownership buttons, already a Known Issues bullet, which ADR 0026 decision 5 replaces. Check 7 is the platform's tab bar, closed by ADR 0027 and the 2026-09-04 fix. Check 8 found the one thing the audit was expected to find: ADR 0026 section 7 describes the entry transition as pieces that "travel" from their Today box to their detail box while the stage tint fades "over the same interval", which names a duration for a movement that Law 7's 2026-09-07 note classifies as spatial. The ADR now carries a dated amendment: the travel and the captions' arrival are the spatial spring role, the tint fade is effects motion on a duration token, and the fade ends no later than the pieces settle rather than sharing an interval with them. No mockup was redrawn.

- **Settings as one native grouped list** (2026-09-07): [ADR 0030](adr/0030-settings-as-a-native-grouped-list.md) reworked after the first lane's separate fixed-height lists clipped rows on the Simulator. Each of Settings, Notifications and AI status now has one full-height `Host` and one SwiftUI `List`, with `Section`s instead of estimated group heights or an enclosing `Screen` scroll view. Root order is Language/Appearance, Notifications/AI status, then About you; only About you has a sentence-case `bodyStrong` heading in `textSecondary`, hosted through `RNHostView` with a 16-point leading inset, followed by its native section footer. The version line is the final section footer, centred, `caption`, secondary and tabular, with trailing scroll space. `expo-constants` supplies `Constants.expoConfig.version` and the binary's iOS `Constants.platform.ios.buildNumber` (confirmed in the installed native source); Android uses `platform.android.versionCode` when available, and a missing build falls back to the localized version-only template without adding a dependency. About you remains provisional: it opens the existing clothing preference picker until ADR 0015 supplies Gender and Birth date, and its footer describes only catalog selection. Notifications retains opt-in, busy and denied-permission behavior, with the Open Settings action in the same section; AI status retains generation mode, bounded check intent, coarse monochrome result, footer and checking overlay. Native inline route headers and the existing pushed pickers are retained. Package inspection corrected the probe note: installed `@expo/ui` 57.0.8 declarations, implementation and Swift registry expose `listStyle`, `scrollContentBackground`, `listRowBackground` and `tint`; the list uses inset grouped style, hides its scroll background over kuyara's ground, and applies `brandPrimary` tint, as does the existing toggle wrapper. Background and tint were verified on the Simulator afterwards, so neither is a package limitation; the universal API still has no sections, so Android falls back to one universal `Host`/`List` without SwiftUI sections and its rendering is unverified. Files: `components/ui/native-list.tsx` now exports `NativeList`, `NativeListSection({ heading, footer, children })` and `NativeListRow`, with shared leading tiles through `RNHostView`, native secondary values, supporting text and tertiary chevrons on navigation rows; `components/ui/index.ts`, the three `features/profile/presentation/*settings-screen.tsx` screens, `localization/messages.ts`, the shared native mock, native-list tests and the affected profile/notification component tests were updated. The earlier lane's routes, native toggle and heading-ink amendment remain. Tests cover one host/list, section order, heading semantics/ink, row intents, chevrons, supporting text, version/build copy, preference propagation and both pushed surfaces' states. Verified with `pnpm --filter @kuyara/mobile test:components` (155/155 across 18 suites), `pnpm --filter @kuyara/mobile test:theme` (15/15), and `pnpm check` (contracts 28/28, Worker 139/139, mobile Node 249/249, typechecks passed and Worker dry-run bundle built); lint retains one pre-existing unused-import warning in `worker-ai-recommendation-mapper.ts`. The theme guard initially caught literal font sizes in the new test assertions; those now reference typography tokens, with the affected `pnpm --filter @kuyara/mobile test:components preference-propagation native-list` rerun passing 10/10. The aggregate bundle succeeded but its default Wrangler log path was sandbox-denied; the bundle was rerun with `WRANGLER_LOG_PATH=/private/tmp/settings-rework-20260907-wrangler.log`. The five scoped grep checks, `git diff --check` and starting-path comparison passed. Ponytail review found no additional complexity to remove. Logs are retained under `/private/tmp/settings-rework-20260907-*`. The unchanged Maestro flow still needs to open `settings-clothing-row` before looking for the existing clothing option ids in the pushed picker, then pop with `settings-picker-back`; native row test ids are forwarded, but Maestro resolution on SwiftUI rows is unverified. Verified by the orchestrator on the iPhone 17 Pro / iOS 26.3 Simulator: the root list at full height with its three sections over kuyara's ground, the About you heading in textSecondary, tiles, values, chevrons and the build-numbered version line, the Notifications surface with its toggle and footer, and the AI status surface with the tinted check row. The earlier Sonnet attempt, three fixed-height lists that clipped their rows, was rejected on the Simulator and reworked by a Codex lane on this design. The Maestro settings flow was updated for the pushed clothing picker (scroll to the row, open it, choose, back). Its first run failed: Maestro cannot resolve a `testID` placed on a SwiftUI `ListItem`, so a row's identifier now lives on the React Native tile view hosted in its leading slot (rows without a tile keep the identifier on the `ListItem`), and with that both flows passed 2/2.

- **Profile, the Closet as its subject** (2026-09-07): [ADR 0028](adr/0028-the-profile-tab-and-the-list-row-anatomy.md) sections 1, 3, 5, 6 and 7 implemented on the Profile tab, on the `ListRow`/`ListRowGroup`/`ListRowTile`/`useTextScaling` primitives that landed the same day. The screen is now, top to bottom: a native large title with the Settings gear as the bar button, set through `<Stack.Screen options={{headerLargeTitle: true, headerRight, headerShown: true}}>` in `app/(tabs)/(profile)/profile.tsx` rather than hand-drawn, because it is chrome the OS owns; a Closet heading row (the word plus the owned count plus a chevron, pressable, opening the Closet with no filter); the rail, a horizontal, edge-bleeding row of up to eight 136 by 170 tiles for the newest owned pieces plus a ninth "All pieces" tile past eight, each tile drawing the photo rung when the item has one and the structural-category glyph otherwise (the silhouette rung and the colour-family fill wait for Today's board, section 7 item 2, and are not added); and one inset `ListRowGroup` holding a Wanted row (hidden only while nothing is owned or wanted, shown the moment something is) and a Location row. The empty Closet (nothing owned or wanted) shows the heading, a sentence, and an "Add a piece" button, the screen's only accent fill. `ListRow` gained one prop, `labelWeight` (default `body`, `bodyStrong` for Location), because the anatomy's Location row needs a heavier label than the anatomy's own default and the primitive had no way to express that; covered by a new assertion in `list-row.component.test.tsx`. The seventh status site, `profile-screen.tsx`'s `checkCircle` recommendations-working row, is deleted with the screen it sat on (section 7 item 4). The route to the Closet list gains an optional `filter` search param (section 7 item 3, shared with ADR 0029): `app/(tabs)/(profile)/wardrobe/index.tsx` reads it and `wardrobe-list-route.tsx` forwards it as `wardrobe-list-screen.tsx`'s new `initialEntryState` prop, which seeds the existing owned/wanted filter state; nothing else in the list screen changed. Several dead Profile localization keys (`piecesSavedUnit`, `categoryCountAccessibilityLabel`, `wardrobeSummary`, `locationTitle`, `locationChangeHint`, `wardrobeHint`, `recommendationsWorking`, `recommendationsNeedLocation`, and the already-unused `ownedLabel`) were removed rather than kept unused, and `theme.test.mjs`'s locale-resolution test was updated onto the two new template keys that replaced them. Three points where the ADR's own text does not match the shipped codebase, found rather than resolved: the "title 22" and "bodyStrong 17" figures assume [ADR 0017](adr/0017-a-retuned-typography-scale.md)'s retuned scale, which is Accepted but still unimplemented, so the Closet heading uses the shipped `title` role at 24; the "trailing `spacing['2xl']` 32" figure conflicts with `Screen`'s actual bottom inset of `safeAreaInsets.bottom + spacing.md` from the ADR 0027 fix, so nothing was added and the ADR's own "Screen owns the inset" sentence was followed instead; and the Turkish "İstekler" decision was applied only to `profile.wantedLabel`, so `wardrobe.wantedLabel` (the Closet filter chip's own copy) still reads "İstediklerim" until the Closet lane (ADR 0029) unifies the two keys the ADR calls for. `ListRow` has no accessibility-hint slot, so the Wanted and Location rows no longer carry the hints their predecessors had; the Closet heading row, not a `ListRow`, keeps one. Files: `apps/mobile/src/features/profile/presentation/profile-screen.tsx`, `apps/mobile/src/app/(tabs)/(profile)/profile.tsx`, `apps/mobile/src/app/(tabs)/(profile)/wardrobe/index.tsx`, `apps/mobile/src/features/wardrobe/presentation/wardrobe-list-route.tsx`, `apps/mobile/src/features/wardrobe/presentation/wardrobe-list-screen.tsx`, `apps/mobile/src/components/ui/list-row.tsx`, `apps/mobile/src/localization/messages.ts`, `apps/mobile/src/theme/theme.test.mjs`. Tests: `profile.component.test.tsx` rewritten (10/10), `list-row.component.test.tsx` (7/7), `wardrobe-list-route.component.test.tsx` (4/4). Verified with `pnpm --filter @kuyara/mobile test:components` (141/141), `pnpm check` (contracts 28/28, worker 139/139, mobile Node 249/249, lint clean, typecheck clean, Worker bundle built), the five scoped `rg` checks from the lane brief, `git diff --check` and `git status --short`. Verified by the orchestrator on the iPhone 17 Pro / iOS 26.3 Simulator (native large title, the gear as a bar button, the heading row, the empty sentence and its single accent fill, the hairline Location group) and by the two Maestro flows, which passed 2/2 through the native header button.

- **The Closet grid** (2026-09-07): [ADR 0029](adr/0029-the-closet-grid.md) implemented on `wardrobe-list-screen.tsx`. The vertical card list is replaced by a two-column grid of 174.5 by 218 tiles on the stage fill (`surfaceMuted`), radius 14, gap 12, inset 16, collapsing to one column of 361 by 280 above `fontScale` 1.5 through the same `useTextScaling().usesStackedLayout` threshold ADR 0028 names, with a `key={numColumns}` remount for `FlatList`'s documented "changing numColumns on the fly" limitation. Each tile is the new `WardrobeGridTile`: the photo, cover-cropped, else `GarmentSlotGlyph` sized off the tile's own shorter side by the rail's 72/136 ratio; the silhouette rung and the colour-family fill still wait for Today's board and are not added. The name line is `label` 15, the subline `caption` 13, exactly the tile's own resolution rule: the user's name then the type as subline, else the type as the name with no subline, else the category as the name with "Type not selected" (`wardrobe.unclassifiedType`, unchanged) as the subline. This is the first screen with a native header: `app/(tabs)/(profile)/wardrobe/index.tsx` now sets `<Stack.Screen options={{headerLargeTitle: true, headerRight, headerShown: true, headerTitle}}>` itself, exactly as `profile.tsx` does, with a plus `IconButton` replacing the faked leading-icon button; no change was needed in `_layout.tsx`, since the per-route `Stack.Screen` override already wins over the group's `headerShown: false`. The owned/wanted filter is the Closet's first `@expo/ui` control, behind a new `components/ui/segmented-control.tsx` wrapper (feature code still never imports `@expo/ui` directly): its API is `SegmentedControl({ options, value, onChange, testID })`, built on `@expo/ui/community/segmented-control` because the universal `Picker`'s `appearance` prop is `'wheel' | 'menu'` only, with `appearance` set from the resolved theme rather than the device's, an explicit 44-point height, `tintColor={brandPrimary}`, and the wrapper firing `haptics.selection()` on an actual change. Category chips sit under it as a new `WardrobeCategoryChip`: "All" then only the categories present in the current segment in catalogue order, `label` 15/600, 40 tall with 2 points of vertical hit slop, a 1-point `borderDefined` outline, the selected chip filled `brandAccent`, no haptic; `Pill` was left alone rather than extended, since it is a display-only badge with a hardcoded non-token type size and no press handling, and giving it all of that would be a larger change than this small primitive. States: loading is the grid's own stage-fill tiles with no artwork; error is a 20-point `dangerInk` glyph, 8, a `bodyStrong` title, 4, a `body` line, 12, a retry button, no chips; empty, per filter, reuses `profile.wardrobeEmpty` and `profile.addPieceAction` verbatim rather than the retired `wardrobe.emptyBody`/`emptyTitle`/`wantedEmptyTitle`/`wantedEmptyBody`/`emptyAction`, which are left defined but now unused rather than deleted, to avoid touching `profile-ui.test.mjs`'s and `wardrobe-application.test.mjs`'s existence assertions on them outside this lane's scope; a background refresh failure keeps its own small inline `dangerInk` banner instead, which the old list also had but without ink or a glyph. The shipped `backgroundColor: item.colorFamily` swatch is removed with the old list and not replaced with a colour table yet. The Turkish "İstekler" decision (ADR 0028 section 5), applied by the Profile lane only to `profile.wantedLabel`, is now applied to `wardrobe.wantedLabel` too, closing that lane's recorded gap. Two verified `@expo/ui` limitations, read from the installed 57.0.8 source rather than guessed, are recorded as contradictions rather than silently worked around: `SegmentedControlProps.tintColor` is applied only on Android (`SegmentedButton.colors`); the iOS implementation never reads it, so `brandPrimary` tinting is Android/web only until the package exposes an iOS tint hook, and the component carries no per-segment accessibility hook at all, so the "counts in each segment's accessibility value" instruction cannot be implemented against the checked type surface and counts are simply not attached, rather than faked. The community component also owns its own internal `Host` with `matchContents={{ vertical: true }}`, forwarding this wrapper's `style` alongside it; the explicit 44-point height is the same fix ADR 0019 already recorded for `matchContents` collapsing inside a `ScrollView`, but it is the one behaviour in this screen not confirmed on a device by this lane. `onBack`/`router.back()` is removed from `WardrobeListScreen` and `wardrobe-list-route.tsx`, since the native back is the stack's own. Files: `apps/mobile/src/app/(tabs)/(profile)/wardrobe/index.tsx`, `apps/mobile/src/features/wardrobe/presentation/wardrobe-list-screen.tsx`, `wardrobe-list-route.tsx`, `wardrobe-category-chip.tsx` (new), `wardrobe-grid-tile.tsx` (new), `apps/mobile/src/components/ui/segmented-control.tsx` (new), `apps/mobile/src/components/ui/index.ts`, `apps/mobile/src/localization/messages.ts`, `apps/mobile/src/features/wardrobe/wardrobe-application.test.mjs` (one structural assertion moved from `<Animated.FlatList` to `<FlatList`, since the native header retires the scroll-linked `StretchyHeader` animation the `Animated` wrapper existed for), `apps/mobile/src/navigation/__tests__/primary-tabs.component.test.tsx` (its direct `WardrobeListScreen` render updated onto the new empty copy and a segmented-control mock). Tests: `segmented-control.component.test.tsx` (new, 3/3), `wardrobe-list-screen.component.test.tsx` (new, 12/12), `wardrobe-list-route.component.test.tsx` (rewritten, 3/3), six now-obsolete `WardrobeListScreen` assertions removed from `wardrobe.component.test.tsx`. Verified with `pnpm --filter @kuyara/mobile test:components` (145/145), `pnpm --filter @kuyara/mobile test:theme` (15/15), the four scoped `rg` checks from the lane brief, `pnpm check` (contracts 28/28, worker 139/139, mobile Node 249/249, lint clean, typecheck clean, Worker bundle built), `git diff --check` and `git status --short`. Verified by the orchestrator on the iPhone 17 Pro / iOS 26.3 Simulator: native large title, back to Profile and the plus bar button, the native segmented control at 44 points, the empty state, then one owned T-shirt as a grid tile under the All and Tops chips with the selected chip as the screen's only accent fill. Two corrections were made at acceptance: the empty state is laid out under the control as on Profile rather than centred in the leftover space, and the refresh control now shows only for a pull, because the route's focus refresh flipped `refreshing` and left the native control visible under the large title. A third, the glyph tile drawing the 24 point PNG at 72 points and blurring, is closed by the large raster class recorded under the glyph redraw entry.

- **The structural-category glyphs, redrawn** (2026-09-07): [ADR 0025](adr/0025-the-garment-board-composition-rule.md)'s open Law 6 item. A Codex lane (gpt-6-astra) drew `top`, `bottom`, `one_piece`, `outerwear`, `footwear` and `accessory` as siblings of the twenty-two silhouettes, 64 viewBox, stroke 1.9, round caps and joins, and measured them against all twenty-two with a stdlib PNG decoder: at 192 px every glyph lies inside the silhouette set's ink range, and the worst pair among the six is 1.40x coverage and 1.37x box density against the 1.42x limit; the shipped PNGs measured 16 to 31 percent ink at 72 px against 9 to 12 for the idiom-pure re-raster. The maintainer accepted the geometry against a contact sheet and decided the raster weight: the repository PNGs are displayed only at 20 to 28 points, where the idiom-pure stroke renders at 0.6 pt, so the 24/48/72 px exports use an optical stroke of 3.75 / 64 (about 1.4 px at 24 px) over unchanged geometry, the way SF Symbols carries weight at small sizes. Only the eighteen PNGs under `apps/mobile/assets/icons/garment/` changed in the repository; `GarmentSlotGlyph` and every call site are untouched. The SVG sources, the `g-cat-*` symbols, the build and measurement scripts and the sheet live beside the composition-rule material outside the repository. Verified on the iPhone 17 Pro / iOS 26.3 Simulator: the Profile rail renders the four category tiles in the new weight, tinted by the theme. Later the same day the Closet grid and the Profile rail drew the glyph at 72 points, where the 24 point export scaled three times and blurred, so a second raster class was added: `assets/icons/garment/large/` holds the same drawings exported idiom-pure (stroke 1.9) at 72, 144 and 216 px, and `GarmentSlotGlyph` reads it above 32 points through `resolveGarmentArtwork`, covered by `garment-slot-glyph.component.test.tsx`. The `primary_top` / `mid_layer` fallback gap and the five accessory silhouettes stay open.

- **The list-row anatomy, written once** (2026-09-07): [ADR 0028](adr/0028-the-profile-tab-and-the-list-row-anatomy.md) section 2's shared row anatomy is now `components/ui` primitives, ahead of any feature screen adopting it. `ListRow` (`list-row.tsx`) takes a leading glyph, a label, an optional trailing value and supporting line, and an optional `onPress`; a pressable row is a button whose accessible name defaults to the label, never assembled from the label and value, and a caller that needs the two joined passes an explicit `accessibilityLabel`. `ListRowGroup` renders the appearance-dependent group (light: no fill plus a `borderDefined` hairline; dark: the Night Layer surface step, no border, both at the Law 3 `radii.card` 20) and inserts a `borderSubtle` separator between rows at the text edge. `ListRowTile` (`list-row-tile.tsx`) is exported standalone, a 28 by 28, radius 7 tile with a monochrome glyph in `textPrimary` on an 8%/12% `withAlpha` fill, never a coloured tile, so [ADR 0030](adr/0030-settings-as-a-native-grouped-list.md)'s Settings lane can host the same tile inside an `@expo/ui` `ListItem`'s `leading` slot through an `RNHostView`. `useTextScaling` (`use-text-scaling.ts`) is the one shared hook ADR 0019 called for, returning the raw `fontScale`, the `usesStackedLayout` boolean above 1.5, and the `controlScale` capped at 1.5; `app-text.tsx` and `section-header.tsx` were migrated onto it with no visual change, replacing their own inline `fontScale > 1.5` checks. At `controlScale` 1.5 the tile, glyph and chevron scale to 42, 30 and 30 and the separator moves to 70, matching ADR 0028 section 3 exactly; above `fontScale` 1.5 the trailing value stacks under the label and the chevron stays on the label line. One amendment landed with the primitive: the maintainer re-measured the byAir reference and changed the section-heading ink from `textPrimary` to `textSecondary`, recorded as a dated amendment in ADR 0028's section 2 table and "Where it differed" paragraph; list-row reference check 2, whether `@expo/ui`'s `ListItem` exposes a leading-tile slot, is also updated from unverified to the 2026-09-07 `RNHostView` finding already recorded in ADR 0030. No feature screen was changed; Profile, the Closet and Settings adopt the primitive in the lanes that follow. Verified with `pnpm --filter @kuyara/mobile test:components` (133/133) and `pnpm --filter @kuyara/mobile test:theme` (15/15), the scoped `rg` checks for literal `fontSize`/`lineHeight` and for `@expo/ui`/`expo-haptics` under `features/`, `pnpm check`, `git diff --check` and `git status --short`. Simulator verification of the primitive happens with the first screen that adopts it, in the Profile lane.

- **The `@expo/ui` crash, isolated** (2026-09-07): the prerequisite [ADR 0019](adr/0019-adopting-expo-ui-at-the-control-layer.md) and [ADR 0030](adr/0030-settings-as-a-native-grouped-list.md) left open, closed by the orchestrator between lanes with no application code change kept. Isolated on 2026-09-07 on the iPhone 17 Pro / iOS 26.3 Simulator with five probe screens swapped into the Settings route: importing the group does not crash; `List` and `ListItem` render; `Button` with the `label` prop renders; `Button` given string children terminates the app with an `NSInternalInconsistencyException` from `RCTComponentViewFactory`, "ComponentView with componentHandle (`RawText`) not found", because Fabric has no `RawText` view to mount inside a SwiftUI host. A crash report is written after all (`kuyara-2026-09-07-175305.ips`). `ListItem` takes a plain string or an `@expo/ui` `Text` as its headline, `supportingText`, an `@expo/ui` `Text` in `trailing`, and an `RNHostView` in `leading` holding kuyara's own 28 by 28 tile with a tinted PNG glyph, which verifies list-row check 2 at runtime. A React Native `Text` placed directly in a slot does not crash but breaks the row layout; the rule is that slot content is `@expo/ui` components or an `RNHostView`, never bare React Native views or strings on `Button`. Settings can now be built on the native list.

- **The English Closet label** (2026-09-07): every user-visible English "Wardrobe" string in the mobile localization catalog now says "Closet", including navigation, onboarding, Profile, list, form, error, destructive-action and accessibility copy. Turkish remains "Gardırop", and localization keys, domain names, SQLite tables, route segments, types, file names and test ids remain unchanged. Verified with the scoped `rg` checks, `pnpm --filter @kuyara/mobile test:components`, `pnpm check`, `git diff --check` and `git status --short`. The two Maestro flows were re-run by the orchestrator afterwards on the iPhone 17 Pro / iOS 26.3 Simulator and passed 2/2.

- **Profile, the Closet and Settings, designed and closed against the list-row reference** (2026-09-07): a design milestone; no application code changed and no dependency was added. The three target sheets drawn on 2026-09-04 were measured against the eight-check list-row reference adopted the same morning and failed four of them: bare glyphs where the reference has a monochrome tile, separators at the container edge, Settings headings inside the native group, and no group container on Profile. One shared renderer was corrected once, so all three sheets moved together; a first browser measurement then found the row chevron scaling to 62 points at the largest accessibility size and breaking "Wanted" mid-word, which set the `min(fontScale, 1.5)` cap on row controls. The maintainer's own byAir screenshots were measured at 3x afterwards and agreed within a few points (group inset 16, separator 54 from the group edge, row pitch 54, sentence-case headings outside the group, a centred version line last), and three follow-ups closed the same day: the Settings version line, the Profile group radius at Law 3's 20, and a 12 point heading-to-group gap. Accepted as [ADR 0028](adr/0028-the-profile-tab-and-the-list-row-anatomy.md), [ADR 0029](adr/0029-the-closet-grid.md) and [ADR 0030](adr/0030-settings-as-a-native-grouped-list.md), which also amend [ADR 0015](adr/0015-gender-and-age-band-in-the-profile.md) from birth year to birth date with no user-facing age category. The Turkish wanted label became "İstekler" because the twelve-letter first choice could not fit the label column at `fontScale` 3.118.

- **The tab bar's selected state, and the Dynamic Type answer** (2026-09-04): the first implementation task out of the redesign's approved queue, and the first application code change since the redesign began. `iconNames` gains three outline entries and `primary-tabs.tsx` now passes `sf={{ default, selected }}` per tab plus a `labelStyle` on `<NativeTabs>` that sets the selected label to the `label` typography role's weight, guarded to iOS with `Platform.select`. The outline variants are new keys rather than a reshape of the existing three, because `tabWeather` is also rendered outside the tab bar at `weather-screen.tsx:465` through `SymbolView`, whose `name` prop has no selected variant. Two facts settled the platform split. The installed `AndroidSymbol` union, 4055 names, contains no filled counterpart for the weather glyph, only `home_filled` exists among the three, so pairing two tabs and not the third would read as a bug; and Material 3 Expressive states the navigation bar's label is no longer bolded when selected. Android therefore keeps one symbol per tab and relies on its Material active indicator, which is recorded as an accepted limitation of an unverified platform rather than worked around. The Simulator run corrected the ADR twice. iOS 26 draws a **selection capsule** behind the selected tab, so the pre-existing state was not signalled by colour alone as [ADR 0027](adr/0027-the-app-shell-and-its-three-tabs.md) first claimed; the accurate defect is that the application contributed no signal of its own and Law 6's "fill carries state" was ignored. And the Dynamic Type question ADR 0012 opened at the migration is now **answered and closed**: at `accessibility-extra-extra-extra-large` the page content scales dramatically while the three tab labels stay at their normal size and do not truncate, because UIKit does not apply Dynamic Type to tab bar labels at all. Verified with `pnpm check`, `pnpm --filter @kuyara/mobile test:components` at 125 tests, and one iOS Simulator run that exercised all three tabs. The new component assertions were confirmed to fail against the pre-fix source before being accepted.

- **The bottom inset has one owner across feature screens** (2026-09-07): the second shell defect from [ADR 0027](adr/0027-the-app-shell-and-its-three-tabs.md) section 4 is fixed with the removal option, so `Screen` remains the single owner and the rule is grep-enforceable. The `content` styles in Today, Weather, Profile, Onboarding, Settings, the preference picker, the wardrobe item form and the garment type picker no longer set `paddingBottom`; every one now resolves the bottom edge to `safeAreaInsets.bottom + spacing.md`. A new `screen.component.test.tsx` pins the primitive at a non-zero bottom inset both with and without caller content layout, while the existing Today and wardrobe component suites pin the loaded Today screen, both wardrobe form modes and the garment type picker at the same 46 point result. The new feature assertions were run against the pre-fix source first and failed at 32 points for Today and both form modes, and 16 points for the garment type picker, before the eight removals restored them to green. Verified with `rg -n "paddingBottom" apps/mobile/src/features`, `pnpm --filter @kuyara/mobile test:components` (127/127), `pnpm check`, `git diff --check` and `git status --short`; the only production feature grep hit is the explicitly separate `wardrobe-list-screen.tsx` FlatList, alongside the required test assertions. Verified on the iPhone 17 Pro / iOS 26.3 Simulator by the orchestrator: Settings and the garment type picker, scrolled to their ends, now rest at the same clearance above the native tab bar, and no last row sits under it.

- **The app shell and its three tabs** (2026-09-04): a design milestone plus a read-only feasibility check against the installed packages; no application code changed and no dependency was added. The design half was quick and the feasibility half found two defects in shipped code that mattered more ([ADR 0027](adr/0027-the-app-shell-and-its-three-tabs.md)). First, the selected tab is signalled by colour alone: `iconNames` maps all three tabs to filled SF Symbols, `house.fill`, `sun.max.fill` and `person.fill`, and one symbol is passed for both states, so the icon is identical whether or not its tab is selected and `tintColor` carries the whole signal. That fails Law 4's "colour is never the only signal" in the one place every screen inherits. Second, `Screen` computes `safeAreaInsets.bottom + spacing.md` but merges the caller's `contentContainerStyle` last, so a screen's own `paddingBottom` replaces that inset rather than adding to it; eight screens do that, seven passing a flat `spacing['2xl']` and the garment type picker passing `spacing.lg`, rendering 32 or 16 points where 46 was intended. Both fixes are approved and sequenced ahead of the category-glyph redraw. The fix for the first needs no custom tab bar and does not reopen [ADR 0012](adr/0012-adopting-expo-router-native-tabs.md): the installed expo-router documents `sf={{ default: "house", selected: "house.fill" }}` as its own worked example, and `labelStyle` takes a `{ default, selected }` pair whose style type includes `fontWeight`. A selection capsule was drafted as the second signal and withdrawn, because nothing in the package draws one. The recorded content inset is a rule rather than a number, because the bar's height is not exposed anywhere in the package's type surface and the platform already clears it, automatically on iOS for the first nested scroll view and through a safe-area wrapper on Android: a feature screen never sets `paddingBottom` on `Screen`. The Android question was answered too, and the answer is that there is no counterpart to Liquid Glass: `NativeTabs` resolves to `react-native-screens`' `Tabs.Host`, whose bar subclasses Material's `BottomNavigationView` with a solid `colorSurfaceContainer` background, and Material 3 defines no translucent material for the navigation bar at all. That splits the second selected-state signal, because Material 3 Expressive states the bar's label is no longer bolded when selected, so the weight change is iOS-only and Android keeps the Material active indicator it already has. Chrome was settled at an icon and a label: icons alone buy no height, since the bar height is the platform's, and a badge would be the only saturated fill on the screen with nothing to count. Two items stay open: ADR 0012's Dynamic Type re-check, which the repository never recorded an answer to and which stays open until it is verified on the Simulator, and a touch-target measurement, which Jest cannot supply because the component test mocks the native tabs module entirely.

- **The recommendation detail surface** (2026-09-04): a design milestone; no application code changed and no dependency was added. [ADR 0021](adr/0021-direction-e-a-visual-first-design-language.md) took the garment names off Today and deferred five things to a detail surface it did not design. Two of the five were not in the product. Nothing in the contract, the domain or the persisted snapshot produces a per-slot substitution, because the composer emits three whole outfits and the AI returns only option and archetype identifiers, so substitutions are now recorded as a future product decision rather than designed around ([ADR 0026](adr/0026-the-recommendation-detail-surface.md) section 6, amending ADR 0021 section 7). Ownership has only action copy, "I own it" and "I want it", so four state strings were approved and deliberately not added yet, since unused localization keys are the speculative infrastructure the repository rules forbid. The other three were cheaper than expected. `OutfitRequirementEvaluation.suppliedByCandidateKeys` already records which garment satisfies which requirement, and the snapshot stores only `{archetypeId, garments:[{slot, layerRole, candidateKey}]}` and recomposes the outfit on read, so those evaluations are rebuilt on every load: per-piece reasoning needs no schema change, no migration and no contract change, only a presentation join over data the domain currently discards. The design itself rests on one move. The board is [ADR 0025](adr/0025-the-garment-board-composition-rule.md)'s `compose()` run with a second parameter set rather than a second layout, so both surfaces produce the same family, arrangement and reading order, and the transition between them moves the pieces instead of cross-fading two pictures. Detail then spends on captions the margin and gutter the composition already reserved. The board sits on the page ground rather than the condition-tinted stage, because Law 3 forbids secondary copy there and a detail surface is entirely secondary copy; the weather keeps one quiet tinted recap row, and the tint draining away is what the transition animates. Reasoning is organised by weather requirement with each row naming the garments that answer it, rather than one row per garment, which is the device that stops the screen returning to the five-row list the redesign diagnosed as a cause of flatness. Captions use the slot label rather than `layerRole`, which would print "standalone" under a pair of jeans. Evidence: the screen in English and Turkish, light and dark, with zero caption collisions and zero spill in all four, measured against each piece's drawn box rather than its viewBox container. The mockups are kept outside the repository.

- **The garment board composition rule** (2026-09-04): a design milestone; no application code changed and no dependency was added. [ADR 0021](adr/0021-direction-e-a-visual-first-design-language.md) made the garment composition Today's hero and then recorded, against itself, that the rule producing one was unbuilt: the spike had a single board, five hand-tuned percentages, repeated unchanged on every screen. It recorded a second obstacle in the same list, that equal layout boxes do not produce equal perceived size because silhouette paths fill different proportions of their viewBox. Both are now closed by [ADR 0025](adr/0025-the-garment-board-composition-rule.md), specified in [`design/garment-board.md`](design/garment-board.md). Artwork is sized from its own drawn bounds rather than its container, by the geometric mean of that box, and the metric is deliberately the bounding box and not the ink: ADR 0021 makes the silhouette a replaceable slot, and a line drawing replaced by a photograph gains roughly three times the ink at identical size, so an ink metric would re-break the layout the first time the artwork improved. A four-step ladder sizes the slots, with footwear the stated exception because shoe aspect ratios span 2.86 for a sneaker to 0.94 for an ankle boot and a shoe is recognised by the length of its profile. There are two placement families and one predicate between them, whether the outfit carries a layer at all, since footwear is always present and never earns a column of its own. The stage's height is derived from the composition and clamped to 0.80 to 1.16 of its width, because a dress tall enough to span a two-anchor core would be drawn 0.55 wide and a fixed stage yields either a squashed one-piece or an empty band; that is the rule's most reversible part and the one to revisit if native validation shows the jump between outfits is distracting. The composition is then placed by its ink centroid rather than its bounding box, which lifted a two-piece board's weakest half from 4.8% ink coverage to 23.3%. Nine silhouettes were added, bringing the set to twenty-two drawings covering all 27 outfit-eligible catalog types; `dress` and `jumpsuit` were not optional, since without them a one-piece look could not be drawn at all. Evidence: ten slot lists covering every shape the composer can emit, generated from the slot list alone and rendered in both appearances, with overlap 0 and clipping 0 on all ten, anchor parity by drawn area 1.000 against 1.654 for the same board sized by container width, and a weakest half of 0.144. Two findings were left open rather than absorbed. The six shipped structural-category glyphs are drawn far heavier than the silhouettes, measured at 1.86x the ink of the anchor beside them, which is a live failure of Law 6's one-idiom bullet recorded rather than waived; and those six categories cannot separate `primary_top` from `mid_layer`, so an all-fallback board draws the same shape twice at two sizes. Neither blocks goal 2. The rule's reference implementation and rendered boards are kept outside the repository.

- **Product direction, analytics, and licensing sync** (2026-09-04): a documentation and licensing milestone; no application code changed and no dependency was added. Three long-standing statements were corrected rather than extended. kuyara had described itself as local-first in every durable document, including a section heading in `AGENTS.md` whose first rule read that SQLite "is not a temporary database to be removed when remote sync is added"; that wording defended a correct engineering rule but stated it as a product identity, and a future agent reading it would have treated a server-authoritative design as a rule violation. The accountless first release is now recorded as a scope decision, with Supabase Auth, PostgreSQL and Storage as the intended backend, Postgres authoritative for account-backed data once accounts exist, and SQLite keeping its read-and-write-first role on the device ([ADR 0022](adr/0022-supabase-is-the-intended-backend-and-kuyara-is-not-local-first.md)). The "no behavioral analytics" MVP rule was revoked: behavioural product analytics is approved with PostHog as the provider, a project-owned `ProductAnalytics` boundary, a closed payload exclusion list that notably forbids reusing `localProfileId` as an analytics identifier, PostHog Error Tracking as the preferred crash-tracking candidate, session replay held as an evaluation item behind privacy masking, and Grafana kept separate as operational observability ([ADR 0023](adr/0023-behavioural-product-analytics-with-posthog.md)). Analytics is sequenced before the first public release, which promotes the privacy policy and the App Store Connect data-collection questionnaire from open prerequisites to release blockers, and leaves one genuinely unresolved question: whether Apple's current rules oblige a consent, revocation, or deletion mechanism. ATT is recorded as not expected to apply, since kuyara has no advertising, no IDFA, and no cross-app tracking, but that is deliberately not written as a finding that no privacy work is required. Finally the licence changed from MIT to the PolyForm Noncommercial License 1.0.0, applied as the exact unmodified official text, after confirming that all 114 commits are the maintainer's under two identities with no `Co-authored-by` trailer and no outside pull request; "open source" became "source-available" wherever it described the project, and the MIT grant on versions published before this date is explicitly not revoked ([ADR 0024](adr/0024-relicensing-to-polyform-noncommercial.md), [`LICENSING.md`](../LICENSING.md)).
- **Redesign direction and its four decisions** (2026-09-03): a research and decision milestone; no application code changed. The first redesign session diagnosed why the screens still read as flat after Milestones A and B applied the design language in full, and the cause was structural rather than chromatic. Four findings, each verified against the tree: the application sets `headerShown: false` in both `app/_layout.tsx` and `navigation/primary-tab-stack.tsx`, so outside the tab bar it draws none of iOS 26's chrome and hand-rolls three different header patterns instead, meaning [ADR 0012](adr/0012-adopting-expo-router-native-tabs.md)'s "let the OS draw it" argument was applied to the tab bar and nowhere else; `Surface` serves simultaneously as hero card, list row, group card, empty state, error state and form section, so shape carries no meaning; `title` and `titleLarge` were both 24 points with nothing between 24 and 40, so Law 1's three emphasis levels were not expressible and most of every screen rendered at 17 or 13; and the primitive layer stopped at `Surface`, whose measured cost is an 824-line wardrobe form, a 637-line Weather screen, three separate owned/wanted controls, two segmented controls, two disclosures, and six independent re-implementations of the same large-text stacking branch. Four decisions followed, each with its own ADR: the type scale is retuned to 56 / 34 / 22 / 17 / 15 / 13 / 10.5 with negative tracking on the three largest roles ([ADR 0017](adr/0017-a-retuned-typography-scale.md)); the weather moves into a condition-tinted ground rather than a whole-page tint ([ADR 0018](adr/0018-the-atmospheric-condition-band.md); the band shape it originally specified was replaced days later by the tinted stage of [ADR 0021](adr/0021-direction-e-a-visual-first-design-language.md), while its arithmetic stands); `@expo/ui`, an unused dependency since the monorepo was scaffolded, is adopted at the control layer and closes the generic-framework deferral ([ADR 0019](adr/0019-adopting-expo-ui-at-the-control-layer.md)); and Law 7's blanket ban on repeating motion is replaced with a conditional rule ([ADR 0020](adr/0020-rewriting-the-motion-law.md)). The atmosphere decision rests on a measurement worth repeating here, because it closes a question three earlier milestones each reopened: a whole-page weather tint is not available in the light appearance at all. The page ground is bounded above by the card at 1.3946:1 and below by `textSecondary` at 4.5:1 and `borderDefined` at 3:1, leaving a usable band of L 0.63499 to 0.70290, **nine of 255 grey levels**, with almost no chroma room at that luminance. Confining the atmosphere to a condition band that carries only `textPrimary` and icons removes both floors and widens the band to L 0.28739 to 0.70290, seventy-two levels. All twenty-six atmosphere values were then derived as sRGB interpolations between two approved brand hexes at recorded ratios and measured: the worst text contrast in the light set is 4.73:1 and in the dark set 12.82:1, every stop clears 4.5:1 for text and 3:1 for icons, and `neutral` equals the page ground exactly, so under that decision no screen outside Today and Weather changed. (The 2026-09-04 decision to apply Direction E app-wide moves the light ground itself; the atmosphere arithmetic is unaffected.) A gradient is a core React Native style prop, so no dependency is added; the phase 0 probe has since run and found that the prop ADR 0018 named silently no-ops, with `experimental_backgroundImage` as the working one on 0.86.2. The three-tab architecture was examined and deliberately not reopened. Mobbin was unavailable during the session (the MCP requires a paid plan) and nothing was inferred from it.

- **The end-to-end suite runs again** (2026-09-03): `pnpm e2e:ios` passes 2/2 after three corrections, and the one regression the previous status entry predicted was not among them. Maestro resolves test ids on native tab bar items without difficulty, verified by a throwaway probe flow that tapped `tab-profile` and `tab-today` and asserted the resulting screens, so [ADR 0012](adr/0012-adopting-expo-router-native-tabs.md)'s move cost the suite nothing. What actually broke was older. `today-screen.tsx` carried its container id only on the populated path, deriving `today-${presentation.kind}-screen` in its early return, so both flows asserted an id that a fresh install never renders: no location is selected on first launch, so Today lands on the unavailable state. The container id is now stable across every branch and the state-specific id moved inward onto the feedback card, which keeps `today-loading-screen` and `today-unavailable-screen` addressable; a component test pins the invariant in both states and was confirmed to fail without the fix. The settings flow was then stale in two ways from the M6.1 restructure: language and appearance are no longer inline controls but value rows pushing their own picker screens, and clothing preference sits last on Settings by design and needs a scroll to reach. The flow now enters and leaves each picker explicitly. It leaves through the picker's own back control rather than a swipe-back gesture, which was measured to pop the entire Profile stack rather than one screen, and that control gained a test id because its only other handle was a localized accessibility label the flow itself changes when it switches to Turkish. The post-relaunch assertions were reordered so the two picker rows are checked before the scroll that pushes them off screen. Verified with `pnpm check`, `pnpm --filter @kuyara/mobile test:components` (123/123), and `pnpm e2e:ios` (2/2).

- **WeatherKit went live** (2026-09-03): the four Apple secrets (`WEATHERKIT_TEAM_ID`, `WEATHERKIT_SERVICE_ID`, `WEATHERKIT_KEY_ID`, `WEATHERKIT_PRIVATE_KEY`) are set on the deployed Worker and `/v1/weather` now returns `origin.sourceId: "weatherkit"`. Configuring the secrets was not sufficient. The adapter had never made a live call, and its first one failed on every response: Apple's REST API serializes `conditionCode` in PascalCase (`MostlyClear`), while `weatherkit-raw.ts`'s table was keyed on the camelCase Swift `WeatherCondition` case names the documentation implies (`mostlyClear`). Every lookup missed, `mapWeatherKitCondition` threw `invalid_response`, and the chain fell back to Open-Meteo without any outward sign, because that is exactly what the chain is built to do. This is the risk that was recorded as an open gap when the adapter was written, now resolved in favour of the REST behaviour over the documentation. The fix lowers the first character during lookup rather than rewriting all 34 keys, so both spellings map and the table keeps the naming Apple's own enum uses; the existing 34-case test now asserts each code in both spellings. Everything else in the adapter was correct against real data: the schema parsed the live body unchanged, the km/h to m/s conversion, the nearest-hour precipitation probability, and the local-day filtering all produced values consistent with Open-Meteo's reading of the same location minutes earlier.

- **WeatherKit at the head of the provider chain** (2026-09-03): Apple WeatherKit is now the first provider, ahead of Open-Meteo and OpenWeather; see [ADR 0014](adr/0014-weatherkit-at-the-head-of-the-provider-chain.md). No new abstraction was added, the existing `WeatherProvider` interface, error kinds, chain, cap wrapper and rate limiter are reused as they are. Three new Worker files: `weatherkit-token.ts` signs the ES256 bearer JWT with WebCrypto (`crypto.subtle`, no Node `crypto` in Workers), caching the token in the signer's closure for its one-hour life and re-signing under five minutes remaining; `weatherkit-raw.ts` holds the Zod schema and the mapper, converting Apple's km/h wind to m/s (the only unit conversion, since temperature is already Celsius and humidity and precipitation chance are already 0-1 fractions) and mapping all 34 `WeatherCondition` codes explicitly, rejecting anything else as `invalid_response`; `weatherkit-weather-provider.ts` requests `currentWeather,forecastHourly,forecastDaily` and maps 429/401/403/400/404 to quota/auth/auth/invalid_request/invalid_request. `currentWeather` carries no precipitation chance, so the current probability comes from the hourly entry nearest `asOf`, as the Open-Meteo adapter already did. `breezy`, `windy`, `hot` and `frigid` map to `clear`, recorded in the ADR as a deliberate reading: they carry no sky information and the contract enum has no member for them. Chain attempts rose from two to three and the per-attempt timeout narrowed from 4,000 ms to 3,000 ms, re-derived from the mobile client's unchanged 10,000 ms abort, so a configured OpenWeather fallback stays reachable; that timeout narrowing is the only behaviour change to the existing two providers. The existing `createDailyCappedWeatherProvider` is reused with an 8,000 call per UTC day limit, half of the 500,000 monthly allowance spread over 31 days. Apple's attribution requirement conflicted with the milestone's "without changing mobile" wording, resolved in the ADR in favour of the narrow reading: `weatherkit` was added to `weatherSourceIds` and the Weather screen gained one localized attribution row linking to Apple's data-source page, while the weather domain model, freshness rules, schema and mappers are untouched. Gated on four secrets (`WEATHERKIT_TEAM_ID`, `WEATHERKIT_SERVICE_ID`, `WEATHERKIT_KEY_ID`, `WEATHERKIT_PRIVATE_KEY`); without them the chain is exactly what it was.

- **Milestone B, platform feel** (2026-09-02): five pieces of platform adaptation on top of Milestone A's design language, which is unchanged.

  *iOS 26.0 minimum.* `apps/mobile/app.json`'s `ios.deploymentTarget` moved from `16.4` to `26.0` per [ADR 0011](adr/0011-minimum-ios-26.md), the CNG iOS project was regenerated so `IPHONEOS_DEPLOYMENT_TARGET = 26.0` and `Podfile.properties.json` agree, and a fresh development build was installed and verified on iPhone 17 Pro / iOS 26.3. The TestFlight path was re-read rather than exercised, as agreed: `eas.json` needs no change, `extra.eas.projectId` is present, `appVersionSource: remote` and the `production` profile's `autoIncrement` plus deployed Worker URL are intact, `submit.production` still relies on EAS-stored App Store Connect credentials with no `credentials.json` in the repo, and `ITSAppUsesNonExemptEncryption: false` still prevents an export-compliance stall. EAS Build runs the same prebuild, so a cloud build inherits 26.0; the consequence, which is the ADR's decision rather than a surprise, is that the next production build stops installing for any TestFlight tester below iOS 26. Note for reproducing the build: CocoaPods 1.17.0 on this machine aborts with `Unicode Normalization not appropriate for ASCII-8BIT` unless `LANG`/`LC_ALL` are set to a UTF-8 locale, and `expo run:ios` still exits 0 when that step fails, so its exit code alone does not prove a build happened.

  *Native Tabs.* `apps/mobile/src/navigation/primary-tab-bar.tsx` was deleted. `primary-tabs.tsx` now renders `NativeTabs` from `expo-router/unstable-native-tabs` with three `NativeTabs.Trigger` children keyed by the existing route groups `(today)`, `weather`, `(profile)`, each carrying its localized `NativeTabs.Trigger.Label`, its `accessibilityLabel`, and the `tab-today` / `tab-weather` / `tab-profile` test ids the Maestro flows use. Icons still come from the frozen `iconNames` map in `components/ui/icon.tsx` rather than new literals, passed as `sf` on iOS and `md` on Android, and the only styling is `tintColor` from `brandPrimary`: no `backgroundColor`, `blurEffect`, or `shadowColor`, because on iOS 26 the OS draws the bar and those props have no effect. `createPrimaryTabDefinitions` moved into `primary-tabs.tsx` so the tab list stays declared once. The component test was rewritten against a mocked module, since native views cannot mount in Jest, and the old "keeps every visible tab label on one line" test was deleted rather than re-expressed against a mock, because label rendering is no longer this code's job. `profile-ui.test.mjs` read `primary-tab-bar.tsx` as source text and was repointed at `primary-tabs.tsx`: its `accessibilityRole="tab"` and `accessibilityState={{ selected: isSelected }}` assertions are replaced by assertions that the file uses `NativeTabs` and keeps each trigger's accessible name and test id, because the native bar now supplies the role and the selected state itself. That is [ADR 0012](adr/0012-adopting-expo-router-native-tabs.md)'s accessibility argument landing in the tests.

  *Haptics.* `expo-haptics@~57.0.2` was added; it ships no config plugin and is linked into the dev build. A single wrapper, `apps/mobile/src/components/ui/haptics.ts`, exports `haptics.impactLight/selection/success/error/warning` plus a `useRefreshOutcomeHaptics(isRefreshing, failed)` hook so the refresh-outcome rule exists once instead of twice. iOS calls `impactAsync`, `selectionAsync`, and `notificationAsync`; Android calls `performAndroidHapticsAsync` with `Gesture_Start`, `Clock_Tick`, `Confirm`, `Reject`, and `Long_Press`; every other platform, web included, does nothing. Every token returns `void`, swallows its promise, and cannot throw into a call site, because iOS silently no-ops under Low Power Mode, an active Camera, and Dictation. The six sites from [`design-language.md`](design/design-language.md#law-8-non-visual-feedback) are wired and nothing else is: the `RefreshControl` on Today and Weather but not their visible refresh buttons, the refresh outcome on both screens, the shared `select` in the Settings preference picker covering theme and language, the two clothing-preference options at the end of Settings, the wardrobe owned/wanted filter chips and the item form's `updateEntryState`, and `showWardrobeConfirmation` when the request is destructive. Each selection site is guarded so re-selecting the current value stays silent. The clothing haptic sits at the Settings call sites rather than inside the shared `PreferenceOption`, which Onboarding also uses, so Onboarding is untouched. `grep -rn "expo-haptics" apps/mobile/src/features` returns nothing. There is no in-app toggle; that is deferred to the OS setting.

  *Two garment icons redrawn.* Only `outerwear` and `accessory` changed, at 1x/2x/3x, authored as SVG and rasterised to transparent PNG like the rest. `outerwear` is now a long-sleeved open coat with a deep V opening and a hem below the t-shirt's, instead of the t-shirt silhouette with a slit that was indistinguishable from `top` at UI size; `accessory` is now a beanie, a dome sitting on a wider cuff band, which is what removes the bag reading, since the old arch-over-a-line was a bag-handle shape. The other four files, the six-value structural category union, and `GarmentSlotGlyph` are unchanged. These stay drawn rather than becoming symbols: as [ADR 0008](adr/0008-expanding-the-visual-vocabulary-for-m6-1.md) records, no SF Symbols release covers `bottom`, so the iOS 26 minimum does not change that decision.

  *Outfit detail ownership flow.* A new pure `resolveGarmentOwnership(garmentTypeId, items)` in `apps/mobile/src/features/wardrobe/domain/garment-type-ownership.ts` is the whole matching rule: a wardrobe item matches a slot when its `garmentTypeId` equals the slot's catalog type, and nothing else; `owned` beats `wanted`; a null `garmentTypeId` never matches, so legacy rows stay harmless; duplicates resolve deterministically by earliest `createdAt` then `id`, and the match carries the row's `itemId` so a caller updates rather than duplicating. `LocalizedOutfitPiece` now carries `garmentTypeId`, which the presentation layer already had and was discarding. `OutfitDetailScreen` gained `ownershipByGarmentType` and `onSetOwnership` props and stays a pure presentation component; the `[id].tsx` route resolves the map and writes through the wardrobe application layer, updating the matched row when one exists and creating an entry with only `garmentTypeId` and `entryState` otherwise. `TodayScreenState` was deliberately not widened, since it has four construction sites. The static blurb above the piece list became a parameterized count line in both languages, and the Turkish action pair is exactly "Bende var" / "İstiyorum". Today renders none of it, with a regression test asserting that. No schema change and no migration: schema v7's `entry_state` already carries this.

  *Law 2 rhythm correction.* The Simulator pass surfaced that the screens still felt airy, and the cause was drift rather than the rule: sixteen styles across eleven files used `spacing.lg` 16 as a vertical `gap` or `marginTop`, while [`design-language.md`](design/design-language.md) Law 2 reserves `lg` for the container inset and makes `md` 12 the default between groups and between sections. Milestone A's rhythm pass had corrected `screen.tsx`'s inset and the `xl`/`2xl` exceptions but never converted the gaps, so every vertical space on every screen ran a third wider than the language specifies. All sixteen moved to `md`; horizontal `gap`s in row containers (`today-screen`'s `outfitList`, `weather-screen`'s `currentHero`, `profile-screen`'s `header`) and every `padding` were left alone, and the spacing scale in `theme.ts` is unchanged. Separately, outfit detail was the only screen in the app placing a `Divider` between top-level sibling cards rather than between rows inside one card, which put 12 + 1 + 12 = 25 points between two cards that already separate by being cards; the divider was removed, taking card-to-card back to 12, and the one test assertion counting those dividers went with it. This matches the mockups' tightest screens, which is where Law 2's numbers came from in the first place.

  Verified with `pnpm check` (contracts 28/28, worker 111/111, mobile Node 243/243, lint clean, typecheck clean, Worker bundle built), `pnpm --filter @kuyara/mobile test:components` (120/120), and an iOS Simulator pass in both appearances and both languages covering the native tab bar's three localized labels and selected state, the ownership flow writing a real Wardrobe row and the summary recounting, Today staying clear of it, and both redrawn icons in the garment type picker.

- **Catalog content revision to version 3** (2026-09-03): settles the catalog questions M6.1 deferred; see [ADR 0013](adr/0013-catalog-content-corrections-and-version-3.md). Four property corrections: `blouse`, `trench_coat`, and `rain_jacket` thermal levels drop, and `gloves` loses its water resistance. Thermal is an additive budget with an over-protection penalty, so overstated values both won slots they should not and inflated penalties elsewhere; the `gloves` change corrects the record only, because the six composition slots include no accessory slot and the five accessory types never enter an outfit. Two types are added, `sleeveless_top` and `leggings`, both applying to `womens` and `mens`, closing the only two gaps no existing type reached: no top with bare arms, and no bottom above `light` thermal. `clothing-taxonomy.md` had justified those gaps by pointing at Wardrobe overrides, which [ADR 0005](adr/0005-catalog-only-recommendation-candidates.md) removed from the candidate set, so nothing was left to close them. `overshirt` stays a `top` and `jumpsuit` stays available to both preferences; both reconsiderations are closed, not deferred. `garmentCatalogVersion` moved from 2 to 3 once, covering every change together, and the hardcoded version in the Worker probe fixture moved with it. Bumping is mandatory whenever content changes: a stale shared AI cache would serve picks the client's `matchesOption` then rejects, silently degrading every user to the deterministic fallback. The user-visible cost is at most one extra loading state on the first launch after the update, because `catalogVersion` is not part of `RecommendationSignals` and a snapshot is discarded only when the content change makes `mapStoredRecommendation`'s recomposition fail, after which Today regenerates on that same launch; offline use is unaffected. `garmentTypeIds` is duplicated in `packages/contracts/src/ai-v1.ts` and `garment-taxonomy.ts`, so both moved together. No schema migration. A new regression test pins at least three composed options for both preferences in a cold-wet and a hot bucket, since lowering outerwear thermal reduces warm-outerwear supply and fewer than three options degrades a whole bucket to the fallback. Measured while writing it: the full catalog now reaches three high-breathability body cores rather than two, because `sleeveless_top` closed that gap. Verified with `pnpm check` and `pnpm --filter @kuyara/mobile test:components`; no Simulator pass, since nothing visual changed.
- **Milestone A, design language implementation** (2026-09-02): applied [`design-language.md`](design/design-language.md) to app code; the language itself is unchanged. `theme.ts` gained the seven approved roles in both appearances, `successInk`/`successContainer`, `warningInk`/`warningContainer`, `dangerInk`/`dangerContainer`, and `borderDefined`, at the hex values [ADR 0010](adr/0010-status-colours-destructive-variant-and-defined-borders.md) measured; `theme.test.mjs` gained them to `requiredSemanticRoles` plus three new guards: the status band and legibility assertions, the `borderDefined` 3:1 non-text assertions on all three planes, and the newly testable shadow contact contrast rule (light `raised` >= 1.20:1, light `chrome` >= 1.35:1, and dark pinned at exactly 1.000:1 as the recorded, accepted defect, because the dark shadow colour equals the dark background). Five of the six status sites now render ink, glyph, and text together instead of `iconSecondary` for all of them: the AI status section splits into info/checking/ok/warning/error with matching glyphs, wardrobe validation and delete errors take `dangerInk` with an `error` glyph, the Weather stale notice takes `warningInk`, and the two informational notices move to `brandAccent`; the generation-mode badge already carried its own tone and needed no colour change. A `destructive` Button variant was added (`dangerInk` fill, `textOnBrand` label) and used at its one current site, the wardrobe delete button; the confirming `Alert.alert` stays the platform's. `borderSubtle` narrowed to decorative dividers, cards, and chrome hairlines; nine component-identifying boundaries moved to `borderDefined`, closing the live WCAG 1.4.11 failure, and the outline Button's border moved from `borderStrong` (7.08:1, reads as text) to `borderDefined` (4.60:1, reads as a boundary). The rhythm pass fixed the root cause first: `screen.tsx` inset every screen at `xl` 24 rather than the `lg` 16 container inset, with `stretchy-header.tsx` matching it, so both moved to 16 together. `spacing.xl` now survives only as the one sanctioned `headerHeight + spacing.xl` clearance per screen, and `spacing['2xl']` only as trailing scroll space, with the wardrobe add button's `paddingLeft` documented as horizontal icon clearance rather than vertical rhythm; the onboarding illustration's doubled margin-plus-gap was removed. The spacing scale in `theme.ts` is unchanged. The emphasis pass gave `AppText` a `tabularNumbers` prop, applied at every changing temperature, time, count, and percentage; moved the three misused `eyebrow` headings on the wardrobe add form and the garment type picker to `bodyStrong`, leaving the wind/humidity/UV row as the only legal `eyebrow` use; dropped Profile's screen title from `titleLarge` to `title` so the owned count is the single hero; and made the generation-mode Pill `bordered` in both modes so only one accent-filled element renders per viewport on Today. Verified with `pnpm check`, `pnpm --filter @kuyara/mobile test:components` (116/116), and an iOS Simulator pass in both appearances and both languages.
- **Design language overhaul** (2026-09-02): a research and decision milestone. No app code changed; implementing the language is separate follow-up work (see Next Approved Milestones). Produced a new canonical document, [`design-language.md`](design/design-language.md), sitting between `visual-identity.md`'s intent and `design-system.md`'s mechanism: nine laws covering the emphasis budget, rhythm, surfaces, colour, typographic tone, iconography, motion, non-visual feedback, and the deferral carve-out. The feedback law was added with Milestone B's haptics decision; the other eight landed with this milestone. The carve-out ([ADR 0009](adr/0009-a-design-language-layer-and-its-deferral-carve-out.md)) exempts role-shaped additions, a colour, typography, spacing, elevation, border, or motion role, from `design-system.md`'s "current product use" requirement, which still governs concrete variants and primitives unchanged. The deferred set was resolved together rather than one item at a time ([ADR 0010](adr/0010-status-colours-destructive-variant-and-defined-borders.md)): status tokens and the destructive button variant are approved, a new `borderDefined` neutral is approved, an info colour role is decided against permanently (info stays `brandAccent`), and the remaining primitive-shaped items, generic text-input/selector/switch/modal frameworks, non-scrollable/keyboard screen behaviour, and platform-color adapters, stay deferred. Seven new colour roles were added and measured: `successInk`/`successContainer`, `warningInk`/`warningContainer`, and `dangerInk`/`dangerContainer` in both appearances, each ink tuned so its contrast against its own `surface` sits within ±0.8 of `brandAccent`'s, plus `borderDefined` (light `#5C7A83`, dark `#527E90`), which closes a live WCAG 1.4.11 failure: an unselected Wardrobe filter chip and an outline button were identified only by a `borderSubtle` boundary measuring 1.515:1 on the white card, below the 3:1 non-text threshold, in both the shipped product and the mockups. The language also makes deliberate, measured departures from the private mockups: the ground stays `#D0DDDC` rather than the mockups' `#F4F6F5` (1.395:1 measured, against the mockups' own 1.085:1); the mockup amber, green, and red are retuned for contrast and band-fit against `brandAccent`; outline controls move to `borderDefined`; the tab bar is drawn, since every mockup screen shows only a single-stub tab bar rather than the three tabs its own file documents; one density system (12/16/12) replaces the mockups' four inconsistent gap and padding values across seven screens; and "wanted" keeps the same accent as "owned" instead of gaining its own status colour, since neither is a system-outcome status. See [`design-language.md`](design/design-language.md) for the full rule set and every measurement.
- **M6.1 second polish round** (2026-09-02): a further pass on top of M6.1's iOS Simulator verification. `GarmentSlotGlyph` no longer draws stacked bars with Views; it renders bundled monochrome template artwork through React Native `Image` with `tintColor`, so the icons take their colour from the semantic theme in both appearances. The artwork lives at `apps/mobile/assets/icons/garment/` as `top`, `bottom`, `one_piece`, `outerwear`, `footwear`, `accessory`, each at 1x/2x/3x, authored as SVG and rasterised to transparent PNG, still keyed by the existing six-value structural category union; `react-native-svg` remains rejected as a native module that would invalidate the existing dev build, and ADR 0008's reasoning for why no symbol font covers all four slots is unchanged. `Pill` gained an optional decorative leading icon, used for the sparkle on the generation-mode badge; with no icon passed it renders as before. Settings' Theme and Language became value rows showing the current selection with a chevron, each pushing a dedicated picker screen (`app/(tabs)/(profile)/settings/appearance.tsx`, `.../language.tsx`) instead of expanding every option inline, roughly halving the screen height; selection behaviour, persistence, and propagation are unchanged, and the clothing preference control still sits last with no leading icon. The Today weather detail card was deleted (`weather-card.tsx` no longer exists): its stat grid, rain timeline, and provider attribution all already existed on the Weather tab, so it duplicated that tab and pushed the other Today options below the fold; provider attribution remains on the Weather screen. Three competing section-heading treatments, large bold, uppercase letter-spaced eyebrow, and plain, collapsed to one sentence-case `bodyStrong` role; the `eyebrow` role remains correct for small uppercase data labels, notably the wind, humidity, and UV stat captions. The Weather refresh control became a compact pill with a short visible label (`weather.refresh`) and the longer sentence retained as its accessible name (`weather.refreshAccessibilityLabel`). A density pass corrected twelve places where the screens used the correct type scale but sat sections 24 or 32 points apart against the mockups' 12, and cards carried 24 points of padding against the mockups' 16, plus redundant heading margins that stacked on top of a container gap; the spacing SCALE in `theme.ts` is unchanged (`xs` 4, `sm` 8, `md` 12, `lg` 16, `xl` 24, `2xl` 32), only which token each screen selects changed, and `screen.tsx`'s bottom inset moved from 16 to 12. The wardrobe add flow was shortened: adding one item previously rendered 59 to 73 individual radio rows in one flat, ungrouped, unfiltered scroll, and `recommend-outfits.ts`/`outfit-composition.ts` were checked and confirmed to never read a `WardrobeItem`, only catalog type defaults, so colour and the seven attribute overrides have no effect on recommendations and exist only to complete the personal record, consistent with the Wardrobe not being a recommendation candidate source. Garment type is now a row pushing its own picker screen (`app/(tabs)/(profile)/wardrobe/garment-type.tsx`), grouped by structural category and filtered by clothing preference through the existing `listGarmentTypesForPreference` predicate, with each type showing its garment icon; colour and the seven overrides moved into a collapsed "Details" section stating that catalog defaults apply. No field was removed and no default changed; the default view went from 59 rows to four controls. Verified with `pnpm check` (worker bundle dry-run executing; contracts 28/28, worker 111/111, mobile node tests 234/234) and `pnpm --filter @kuyara/mobile test:components` (116/116), and on the iOS Simulator.
- **M6.1, mockup alignment** (2026-09-02): closed the gap to the private design mockups approved in [ADR 0008](adr/0008-expanding-the-visual-vocabulary-for-m6-1.md). Light `surface` moved to `#FFFFFF` (`background` stays `#D0DDDC`), measuring 1.395:1, up from 1.255:1; light `elevation.raised` was softened to suit the white card (offset `{0, 4}`, radius 12, opacity 0.1, Android elevation 3); dark theme is unchanged. The M6-era invariant `backgroundElevated >= surface` is retired: light `backgroundElevated` stays Soft Mist and is now the navigation-and-chrome plane between the ground and the card, not the highest plane; see [Elevation ladder](design/design-system.md#elevation-ladder). A new `Icon` primitive (`apps/mobile/src/components/ui/icon.tsx`) wraps `expo-symbols` `SymbolView` behind a single frozen 25-entry map from semantic key to `{ ios, android, web }` names, decorative by default and named only when given an `accessibilityLabel`. Garment slot icons are instead drawn with plain Views through new `GarmentSlotGlyph`/`GarmentSlotTile` primitives, because at the then-pinned iOS 16.4 target no SF Symbol covers `bottom` and every outerwear symbol is past the target, Material Symbols has no glyph for any of the four slots, and adding `react-native-svg` was rejected as an out-of-scope native module; see ADR 0008 for the full rationale, including its update for the iOS 26.0 minimum ([ADR 0011](adr/0011-minimum-ios-26.md)). Today, outfit detail, Weather, Profile, Wardrobe, Onboarding, and Settings were restyled toward the mockups; the tab bar gained icons; Profile gained the location section M6 omitted; Settings gained a masking header. Deliberate divergences from the mockups: the ground stays `#D0DDDC` rather than the mockups' `#F4F6F5`; the mockups' green and amber status colours were not adopted, status stays on icon-plus-text over existing semantic roles; Today's two option cards keep the normal card treatment instead of the mockups' border-only separation, because that 1px border measures 1.51:1 on white, below the 3:1 non-text threshold; Wardrobe keeps its two-segment owned/wanted filter rather than adding the mockups' third "All" chip, which would be a behaviour change; and Settings keeps its inline segmented controls rather than the mockups' value-plus-chevron picker rows, which would be a navigation change. Verified on the iOS Simulator.
- **M6, UI and UX revision** (2026-09-01): the elevation ladder fix at the token level (light `background` moved to `#D0DDDC`, `backgroundElevated` moved to Soft Mist and is now the highest plane) plus the two-level elevation tokens `elevation.raised` and `elevation.chrome`; a `Divider` primitive with full and inset variants; the 40pt `display` typography token now used for one hero value per screen; three-tab navigation with Profile (see [ADR 0006](adr/0006-three-tab-information-architecture.md)); the Profile screen showing wardrobe owned and wanted counts; an owned/wanted filter and form control in the Wardrobe; the clothing preference control moved to the end of Settings; and an icon-plus-text fix, in the AI status section and the preference option control, for states previously signalled by color alone. Deliberately not done: status colors and the destructive button variant stay deferred pending visual identity approval, a tab-bar icon system stays deferred pending brand approval, and the ownership badge on outfit detail stays deferred pending a wardrobe-to-catalog matching rule, which is domain work rather than presentation. See [design-system.md](design/design-system.md#implemented-and-deferred). Verified with `pnpm check` (373/373 Node tests), `pnpm --filter @kuyara/mobile test:components` (116/116), and an iOS Simulator check in both themes and both languages.
- **Schema v7, wardrobe entry state** (2026-09-01): migration v7 adds an `entry_state` column to `wardrobe_items` with values `owned` and `wanted`, `NOT NULL`, defaulting to `owned`; every pre-existing row is preserved as `owned`. This is the data-layer half of the catalog-only recommendation revision; the wanted-list UI is deliberately not part of it and still waits on the Profile tab work below. Also corrects three garment display names: `jeans` TR "Kot pantolon", `closed_shoes` "Klasik ayakkabı" / "Dress shoes", `weather_boots` "Kışlık bot" / "Winter boots". These are display strings only; no garment property and no catalog version changed.
- **Fresh-install recommendation trigger fix** (2026-09-01): on a clean install, Today never produced a recommendation because the generation trigger compared current signals against an in-memory ref that reset to null on every mount, so neither the first location selection nor reopening the app counted as a change. The comparison baseline is now the persisted recommendation snapshot's signals (weather snapshot id, location key, clothing preference, day variant), surfaced on `RecommendationSnapshot` from the already-stored context; no SQLite schema change or migration was needed. The dead `'app-opened'` trigger and the `shouldRefreshRecommendation` helper were removed, and a missing persisted snapshot now yields a `'first-recommendation'` trigger. A relaunch whose four signals are unchanged still generates nothing. Onboarding's third `wardrobePromise` bullet no longer presents the Wardrobe as a recommendation input, in Turkish and English. Verified with `pnpm check` (228/228 Node tests), `pnpm --filter @kuyara/mobile test:components` (109/109), and a Simulator clean install on iPhone 17 Pro / iOS 26.3.
- **AI selects and labels precomposed outfits** (2026-09-01): the AI now selects exactly three outfits from the deterministic layer's precomposed candidates and labels each with an archetype identifier, instead of composing outfits itself. Formality is a catalog property, with a catalog version increment; color harmony is removed from the AI job. The Worker caches the shared AI result under a key derived without personal data. Today and outfit detail show the archetype name as the outfit title. See [ADR 0007](adr/0007-ai-selects-precomposed-outfits.md).
- **Manual refresh affordance** (2026-08-30): pull-to-refresh on Today and Weather, refreshing weather only and never triggering recommendation generation. Today gains a visible refresh button in its stretchy header and a three-state freshness line (last updated, refreshing, refresh failed) on a polite live region; Weather's existing refresh button moves below the location row. The gesture is never the only refresh path, because it is unreachable for VoiceOver and Switch Control. See [Approved manual refresh affordance](product-decisions.md#approved-manual-refresh-affordance).
- **First TestFlight path** (2026-08-30): iOS distribution credentials, production build, App Store Connect app record, and `eas submit` to TestFlight internal testing. Adds `ITSAppUsesNonExemptEncryption: false`, a localized `NSMotionUsageDescription` (CoreMotion is linked transitively by `expo-location`), and `promptToConfigurePushNotifications: false`. Verified live weather on a physical device.
- **N1 mobile notification foundation** (`6e586e9`, 2026-08-29): local opt-in and permission handling, notification-response routing, development-only test action, and schema v6.
- **Milestone 5, real weather providers** (2026-08-29): Open-Meteo primary and OpenWeather fallback with bounded attempts, validation, attribution, and usage controls. See [ADR 0002](adr/0002-real-weather-provider-chain.md).

The interface redesign is in progress. Its direction is settled and its documentation
is amended (steps 1 to 3 below are done). Design goal 1, the composition rule and
silhouette set, is done and accepted as
[ADR 0025](adr/0025-the-garment-board-composition-rule.md), and goal 2, the recommendation
detail surface, as [ADR 0026](adr/0026-the-recommendation-detail-surface.md), and goal 3,
the app shell, as [ADR 0027](adr/0027-the-app-shell-and-its-three-tabs.md); the next piece
of work is
design goal 4, Profile. On 2026-09-07 the three list-shaped goals, 4 to 6, gained a shared
list-row reference and checklist, and Law 7 gained a spatial-versus-effects motion
distinction; see "The list-row reference" below. No application code has changed for the
redesign.

## Next Approved Milestones

An interface redesign that changes information architecture, not only visual
treatment, became the active work on 2026-09-03. It was serialized ahead of everything
else while its scope was still moving.

**The serialization was lifted on 2026-09-04.** With Direction E accepted and the
documentation amended, work that changes no screen may run in parallel with the
redesign's design sessions, provided it has no meaningful file or architectural
contention with them. The redesign remains the primary front; parallel work is
additional, not a substitute, and anything that touches presentation code stays behind
it.

Its direction is settled and recorded. Five ADRs carry it:
[ADR 0017](adr/0017-a-retuned-typography-scale.md),
[ADR 0018](adr/0018-the-atmospheric-condition-band.md),
[ADR 0019](adr/0019-adopting-expo-ui-at-the-control-layer.md),
[ADR 0020](adr/0020-rewriting-the-motion-law.md), and
[ADR 0021](adr/0021-direction-e-a-visual-first-design-language.md), which is the
accepted visual language and amends parts of 0017 and 0018. The three-tab architecture
is untouched: [ADR 0006](adr/0006-three-tab-information-architecture.md) and
[ADR 0012](adr/0012-adopting-expo-router-native-tabs.md) were examined and deliberately
not reopened; only the English label changed, from Wardrobe to Closet.

### What the spike settled, and what comes next

**Steps 1 and 2 are done.** The decisions were recorded on 2026-09-03, and the visual
design spike ran in its own session with no production code. It produced five directions
across the iPhone Simulator and disposable HTML, and the maintainer accepted
**Direction E**, recorded in
[ADR 0021](adr/0021-direction-e-a-visual-first-design-language.md).

The spike also answered both Phase 0 probes early, and turned up a third finding that
neither probe asked for. All three matter:

- `backgroundImage: 'linear-gradient(...)'` **silently no-ops** on React Native 0.86.2 in
  the dev build, in both its string and its object form, with no warning and no type
  error. `experimental_backgroundImage` renders correctly. ADR 0018 §6 names the wrong
  prop.
- `@expo/ui`'s universal components **do mount** in the existing dev build, but
  `Host matchContents` collapses to zero height inside a `ScrollView` and renders nothing;
  an explicit height works. Separately, one component in the universal set terminates the
  app with no crash report, narrowed to the `List` / `Button` import group and not
  isolated further (it was, on 2026-09-07; see Recently Completed). `FieldGroup`, `ListItem` and `Switch` are fine.
- `Host` follows the **device** appearance, not kuyara's resolved theme, so a Light
  preference on a dark device inverts every native control. `Host colorScheme` fixes it.

**Step 3 is done.** The documents the spike moved were amended rather than replaced:
`visual-identity.md` (clothing illustration, palette allocation), `design-language.md`
(Laws 1, 3 and 5), `design-system.md` (the atmosphere role, the light allocation, the
silhouette vocabulary), ADR 0006 (the Closet label), ADR 0017 and ADR 0018 (amendment
headers rather than silent contradiction).

**Step 4 is the next UI/UX sequence below.** The earlier phase list, which assumed a
type-led Today and a full-width atmospheric band, is superseded by it.

### Next UI/UX iteration goals

Each is one focused session ending in reviewable evidence, not production code, unless
its acceptance criteria say otherwise. Goals 4 to 6 design against the 2026-09-04
decision that Direction E applies app-wide: Soft Mist ground, no white-card step, and
separation carried by type and space. Two changes from the obvious order, both from
repository evidence: the composition rule comes first because every board in the spike is
hand-placed and nothing can be implemented without it, and the app shell moves up because
its bottom inset changes the layout of every screen drawn inside it.

**The list-row reference, adopted 2026-09-07.** A third-party iOS app's settings screen
(byAir; the screenshot is kept with the mockups outside the repository) was studied as a
reference for the three list-shaped screens, goals 4 to 6. It is not a target to copy. What
it shows is the platform's inset grouped list idiom, the same one Apple's own Settings app
draws, and kuyara already chose that idiom through
[ADR 0019](adr/0019-adopting-expo-ui-at-the-control-layer.md). The reference was
decomposed into the checks below; the checks, not the screenshot, are what the goals
inherit.

1. Grouped inset rows, one group per section.
2. A leading icon tile: a rounded square with a monochrome glyph, its fill derived through
   `withAlpha` from a semantic role, the glyph sized by Law 6 (20 beside `body`). Verified
   2026-09-07: `@expo/ui`'s `ListItem` accepts an `RNHostView` in its `leading` slot.
3. The separator starts at the text edge, after the tile, not at the container edge.
   `Divider`'s `inset` variant currently starts at 16; the corrected value is a
   `components/ui` decision, never a feature-file one.
4. A trailing value in `textSecondary` followed by a chevron, on one line, stacking under
   the label above `fontScale` 1.5 through the one shared hook ADR 0019 calls for.
5. Section headings stay kuyara's: sentence case, `bodyStrong`, drawn outside any native
   group, so that SwiftUI's uppercase section header never appears (Law 5).
6. One accent fill per viewport (Law 1, already binding). The reference's monochrome
   tiles are why it satisfies this; coloured tiles would not.
7. The tab bar is the platform's ([ADR 0012](adr/0012-adopting-expo-router-native-tabs.md),
   [ADR 0027](adr/0027-the-app-shell-and-its-three-tabs.md)); nothing to design.
8. Motion is classified as spatial or effects, per the Law 7 note added the same day in
   [`design-language.md`](design/design-language.md#law-7-motion). Anything that moves
   between positions or sizes is spatial and is specified as the spring role, not as one
   of the three duration tokens.

How the goals use it:

- **Goal 4 decides the row anatomy, checks 1 to 4, once.** Goals 5 and 6 reuse it
  unchanged. ADR 0019 measured what happens when the same control is written three times.
- Goals 4 to 6 carry the checklist in their acceptance criteria.
- Goal 6 takes the screenshot as its reference and carries two decisions already made:
  check 5, and a version line as the screen's last element.
- Goal 7 measures the spring role on the Simulator, under real Reduced Motion.
- **Goals 1 to 3 are not reopened.** They are audited once against the checklist, as a
  documented pass over the mockups kept outside the repository, and the audit is expected
  to touch nothing but one question: [ADR 0026](adr/0026-the-recommendation-detail-surface.md)
  moves the pieces between Today's board and the detail preset rather than cross-fading,
  which makes that transition spatial, so the audit records whether the ADR's motion
  wording needs to say spring. Any other finding becomes a line in the relevant ADR, not
  a redrawn mockup. The audit ran on 2026-09-07; see Recently Completed.
- Goals 4 to 6 were closed against this checklist on 2026-09-07 (ADR 0028, ADR 0029,
  ADR 0030). Goal 7's validation pass ran on 2026-09-09 over the shipped app (see Recently
  Completed); the bottom inset landed on 2026-09-07 and its double count is one of goal 7's
  findings; the goals 1 to 3 audit ran on 2026-09-07. No application code changed
  for any of this.

Where the reference and Direction E pull apart, recorded so the next session does not
rediscover it: the reference's grouping rests on a card fill step, and
[ADR 0021](adr/0021-direction-e-a-visual-first-design-language.md)'s amendment removes
that step from the light appearance. A native `@expo/ui` group will bring its own step
back in system colours; that is the trade goal 6's acceptance already names as
"visible rather than hidden". Dark is unaffected: the reference uses a black ground and
kuyara uses Night Layer, both with a comparable group step. The palette is locked either
way.

**1. Composition rule and silhouette set. Done, 2026-09-04.**
- Accepted as [ADR 0025](adr/0025-the-garment-board-composition-rule.md) and specified in
  [`design/garment-board.md`](design/garment-board.md). The remaining goals keep their
  numbers so that [ADR 0021](adr/0021-direction-e-a-visual-first-design-language.md)'s
  reference to "design goals 4, 5 and 6" stays true.
- *Left open, deliberately.* The six structural-category glyphs need redrawing at the
  silhouette's weight; that is separate follow-up work and does not block goal 2.

**2. Recommendation detail. Done, 2026-09-04.**
- Accepted as [ADR 0026](adr/0026-the-recommendation-detail-surface.md), with the detail
  preset added to [`design/garment-board.md`](design/garment-board.md) section 9.
- *Left open, deliberately.* Substitutions are recorded as a future product decision
  rather than designed, because no layer of the product produces one. Four ownership state
  strings are owed when the screen is implemented. The control that changes ownership is
  still undecided.

**3. App shell and three-tab navigation. Done, 2026-09-04.**
- Accepted as [ADR 0027](adr/0027-the-app-shell-and-its-three-tabs.md). Three tabs and the
  shipped labels were already correct; two non-colour selected-state signals were not, and
  the recorded content inset turned out to be a rule about ownership rather than a number.
- *Two defect fixes approved, and sequenced first.* See "Implementation the redesign has
  approved" below.
- *Closed since.* ADR 0012's Dynamic Type re-check was verified on the Simulator on
  2026-09-04: UIKit does not scale tab bar labels, so they neither grow nor truncate at the
  largest accessibility size. The tab bar still has no touch-target measurement.

**4. Profile. Done, 2026-09-07.**
- Accepted as [ADR 0028](adr/0028-the-profile-tab-and-the-list-row-anatomy.md): the Closet
  is the subject, with a rail of the user's own pieces as the hero and one inset group for
  the Wanted and Location rows. The list-row anatomy, checks 1 to 5 of the reference, is
  decided there once and goals 5 and 6 inherit it. The sheet was drawn on 2026-09-04,
  measured against the checklist on 2026-09-07, corrected in four places (leading tile,
  separator inset, group container, a capped control scale at accessibility sizes), and
  approved visually the same day.
- *Implemented, 2026-09-09.* The rail's silhouette rung and colour-family fill now use the
  approved board vocabulary through the shared tile artwork ladder.
- *Left open, deliberately.* Five accessory silhouettes are wanted and not approved.
  The Turkish register question stays recorded below.

**5. Closet. Done, 2026-09-07.**
- Accepted as [ADR 0029](adr/0029-the-closet-grid.md): a two-column grid of the rail's
  tile, a native segmented owned/wanted filter without counts, kuyara-drawn category chips,
  a plus bar button, and one tile contract that lets the photo, silhouette and glyph states
  share a frame. The colour-family fill on silhouettes is approved as content colour for
  the rail and the grid only; the shared silhouette rung and fill landed on 2026-09-09.
- *Left open, deliberately.* The add and edit form and the photo pipeline; accessory
  silhouettes, so an accessories tile is a glyph tile until five drawings are approved.

**6. Settings. Done, 2026-09-07.**
- Accepted as [ADR 0030](adr/0030-settings-as-a-native-grouped-list.md): a native inset
  grouped list in system colours over kuyara's ground, kuyara's sentence-case headings
  outside the group, the shared leading tile, Notifications and AI status as their own
  surfaces, About you last with the birth date shown as a date, and a centred version line
  from one `{version}` / `{build}` template key. The build-number API, `expo-constants` or
  `expo-application`, is still verified at implementation. Provider and model identity
  stay hidden; the sheet's variant that shows them is not the target.
- *Left open, deliberately, except the first, which closed on 2026-09-07 (see Recently Completed).* The `@expo/ui` `List` / `Button` crash isolation, owned by
  goal 7; Android rendering; whether a separate saving line survives against the real
  component.
- *Amendment.* ADR 0028 and ADR 0030 amend [ADR 0015](adr/0015-gender-and-age-band-in-the-profile.md):
  the fact collected is the birth date, shown only as the locale-formatted date, with no
  user-facing age category anywhere. [ADR 0031](adr/0031-dress-style-is-the-formality-signal.md)
  later retired the derived band and made birth date inert in product logic.

**7. Cross-screen convergence and the native port spike**
- *Purpose.* Prove the language holds across every surface, then port it to a real Expo
  spike to validate rather than redesign.
- *Design question.* What breaks first when the whole set is seen together at the largest
  Dynamic Type size, in Turkish, in dark? (The tab bar half of this is answered:
  [ADR 0027](adr/0027-the-app-shell-and-its-three-tabs.md) section 5 records that UIKit
  does not scale tab bar labels at all, verified at the largest accessibility size.)
- *Evidence.* A native Expo spike, disposable, with genuine Simulator captures.
- *Acceptance.* The full validation list in
  [ADR 0021](adr/0021-direction-e-a-visual-first-design-language.md) is exercised: Dynamic
  Type, both languages, genuine dark mode set through the app's own preference, VoiceOver,
  Reduced Motion, touch targets, safe areas, contrast, silhouette legibility at small
  sizes, horizontal scrolling on Weather, and progressive-disclosure accessibility. Native
  correctness is proven or the specific failures are recorded. The spatial spring role
  from Law 7 is measured here, on the garment entrance and the Today-to-detail transition,
  with its static end state confirmed under real Reduced Motion; and whether `@expo/ui`'s
  `ListItem` can carry the leading icon tile (check 2) is answered.
- *Out of scope.* Production implementation. That is a separate milestone with its own
  gates.
- *Done, 2026-09-09, as a validation pass over the shipped app rather than a disposable
  spike, because every redesigned surface was implemented by then.* Every axis has captures
  or an explicit not-run line, the findings table and the ordered follow-up list are in
  Recently Completed, and the two owed answers are recorded there: the spring role is not
  measurable until ADR 0026 decision 7 lands, and the `ListItem` leading tile still holds.
  Accessibility Inspector and real VoiceOver did not run and are the ninth follow-up.

Weather is not in this list. Its content direction is accepted as it stands and only
inherits the shell.

### Implementation the redesign has approved

Design goals 1 to 3 each ended with implementation work that is decided but not built.
The order was set on 2026-09-04: **the two shell fixes come first**, because every screen is
drawn inside the shell and neither depends on anything the remaining design goals decide.
Both shell fixes are now done.

1. ~~**The tab bar's selected state.**~~ **Done, 2026-09-04.** Implemented and verified on
   the Simulator; see the entry in Recently Completed. iOS now changes the icon from
   outline to filled and sets the selected label to the `label` role's weight. Android
   keeps one symbol per tab, because the installed `AndroidSymbol` union has no filled
   weather glyph, and relies on its Material active indicator.

2. ~~**The bottom inset.**~~ **Done, 2026-09-07.** [ADR 0027](adr/0027-the-app-shell-and-its-three-tabs.md)
   section 4. The removal option was taken because it is grep-enforceable and keeps
   `Screen` as the single owner. `Screen` now resolves every feature screen's bottom edge
   to `safeAreaInsets.bottom + spacing.md`. Goal 7 measured the result on 2026-09-09: the
   last content sits 110 points above the tab bar, not 12, because the automatic content
   inset and the padding both count the bar; see Known Issues.

3. ~~**The structural-category glyph redraw.**~~ **Done, 2026-09-07.** Redrawn in the silhouette idiom with an optical raster weight for the row and tile class; see the entry in Recently Completed.
   [ADR 0025](adr/0025-the-garment-board-composition-rule.md)'s open item. The six shipped
   category glyphs are drawn far heavier than the silhouettes and currently fail Law 6's
   one-idiom rule. No screen depends on them, which is why they sit behind the shell fixes.

4. ~~**The three list-shaped screens.**~~ **Done, 2026-09-07.** Profile
   ([ADR 0028](adr/0028-the-profile-tab-and-the-list-row-anatomy.md)), the Closet
   ([ADR 0029](adr/0029-the-closet-grid.md)) and Settings
   ([ADR 0030](adr/0030-settings-as-a-native-grouped-list.md)) all landed 2026-09-07 on the
   shared `ListRow`/`ListRowGroup`/`ListRowTile` anatomy and, for Settings, the new
   `NativeList`/`NativeListSection`/`NativeListRow`/`NativeToggle` wrappers over it; see the three
   Recently Completed entries. What still waits: About you's Gender and Birth date rows
   on ADR 0015's unimplemented phases, and the five unapproved accessory silhouettes.
   The rail's and grid's silhouette rung and colour-family fill landed on 2026-09-09.

5. ~~**Today's garment board.**~~ **Done, 2026-09-08.** [ADR 0025](adr/0025-the-garment-board-composition-rule.md)
   and the Direction E tokens; see the entry in Recently Completed. It made the detail
   preset available for the recommendation detail surface.

6. ~~**The recommendation detail surface.**~~ **Done, 2026-09-09.** [ADR 0026](adr/0026-the-recommendation-detail-surface.md)
   decisions 1 to 6; see Recently Completed. Decision 7's entry transition stays open.

~~Also owed, and small: the Simulator verification of tab label behaviour at accessibility
text sizes that ADR 0012 asked for and ADR 0027 keeps open.~~ Closed on 2026-09-09 by goal
7's pass: the labels stay at their normal size at the largest accessibility size in every
capture, and each tab item measures 94 by 54 points in the accessibility hierarchy, the
number ADR 0027 said only a Simulator could supply.

### Unblocked, and independent of the redesign

Unblocked as of 2026-09-04, and each may run in parallel with a design session. The
first two change no screen and touch neither `apps/mobile/src/features` presentation
code nor the design tokens, so they have no contention with the redesign at all. The
third changes visible strings and their assertions but no layout, so it is sequenced
against live screen work rather than run blindly beside it.

1. ~~**Gender and age band, everything below the UI.**~~ **Done, 2026-09-08.**
   ADR 0015 phases 1, 2, 4 and 5 landed, then [ADR 0031](adr/0031-dress-style-is-the-formality-signal.md) superseded the band before release. The current strict request carries optional `dressStyle`, with absence treated as `smart`; Worker and mobile deployment acceptance remains with the orchestrator. Phase 3 is complete under item 4 below.

2. ~~**Real location selection, below the UI.**~~ **Done, 2026-09-08.** The Worker
   place-search route, shared contract, validated manual-id shape, mobile search data
   source and schema v9 display-name persistence are ready. Milestone 5 implements
   the native picker and legacy sample selections remain readable through their persisted name.

3. ~~**The Closet label, English copy only.**~~ **Done, 2026-09-07.** Every visible
   English use now says "Closet", while Turkish copy and all internal `wardrobe` names
   remain unchanged.

### Held behind the redesign, and shaped by it

These are decided; what the redesign restructures determines how they are built.

4. ~~**Gender and birth date, onboarding and Settings.**~~ **Done, 2026-09-08.** Phase 3 of ADR 0015 as amended on 2026-09-07 is implemented. [ADR 0031](adr/0031-dress-style-is-the-formality-signal.md) extended it with required dress style and made birth date inert in product logic; the Settings half follows [ADR 0030](adr/0030-settings-as-a-native-grouped-list.md).

5. ~~**The location picker screen**, completing milestone 2.~~ **Done, 2026-09-08.** The dedicated native `/weather/location` route replaces the inline sample picker; see Recently Completed.

6. ~~**Location in onboarding, and an honest empty state.**~~ **Done, 2026-09-08.** See
   [ADR 0016](adr/0016-location-in-onboarding-and-an-honest-empty-state.md) and Recently
   Completed.

### Not blocked by any of the above

7. **Local weather alerts (N2).** Phases 1 and 2 landed on 2026-09-09 under
   [ADR 0032](adr/0032-local-weather-alert-rules.md): the planner, the gateway, the
   schema v11 ledger and the observer that reschedules after each persisted snapshot and
   each opt-in or permission change. Phase 3 remains: the best-effort
   `expo-background-task` refresh (57.0.16 aligns with SDK 57), a dependency decision and
   a physical-device check the Simulator cannot give. No server or push token; see
   [ADR 0004](adr/0004-notifications-in-the-mvp.md).

Do not combine these milestones merely for convenience. Server-sent push (N3) remains
deferred and requires its own ADR.

### Analytics, and what it makes a release prerequisite

Approved 2026-09-04 by [ADR 0023](adr/0023-behavioural-product-analytics-with-posthog.md),
which revoked the earlier no-analytics rule. Analytics is sequenced **before the first
public App Store release**, so items 9 and 11 below become submission blockers rather
than follow-up work. Items 8 and 9 were done on 2026-09-09 as documents; nothing is installed, and
items 10 to 14 still each need their own approved task.

The taxonomy sits downstream of the redesign. Screen-name and navigation events depend on
the information architecture the redesign settles, so writing it against screens that are
about to change would be wasted work. The order below reflects that, not a schedule; no
dates are assigned.

8. ~~**Analytics design and event taxonomy.**~~ **Drafted, 2026-09-09**, as [`docs/analytics-taxonomy.md`](analytics-taxonomy.md); see Recently Completed. Awaiting review. The event and property schema, reviewed as
   its own artifact before any integration. Covers the areas ADR 0023 enumerates, keeps
   properties structured, language-independent and low-cardinality, and encodes the
   payload exclusion list. Includes deciding whether an anonymous analytics identity is
   needed at all; `localProfileId` may not be reused as one.

9. ~~**Apple privacy, consent, revocation, and deletion requirements.**~~ **Done, 2026-09-09**, as [ADR 0033](adr/0033-apple-privacy-obligations-for-first-party-analytics.md): a consent surface before the first event and an unprominent withdrawal control on a Settings Privacy surface are now part of item 10's definition of done; see Recently Completed. Verify against
   current official Apple documentation what App Privacy disclosure, consent, retention,
   deletion, and revocation actually require for first-party product analytics. ATT is
   not expected to apply and no ATT prompt is planned, but that is not a finding that no
   privacy work is required. The outcome decides whether a consent or revocation
   mechanism must exist, which the product would prefer to avoid but does not get to
   decide. Blocks item 10.

10. **PostHog product analytics integration.** The `ProductAnalytics` boundary, one
    PostHog adapter behind it, and the taxonomy's call sites. No SDK calls in features.

11. **App Store privacy disclosure and privacy policy.** Update the privacy policy URL
    and the App Store Connect data-collection questionnaire to describe analytics
    collection accurately, before any analytics-enabled release. This joins the existing
    submission prerequisites recorded under Known Issues.

12. **PostHog Error Tracking integration.** Automated crash, exception, and error
    tracking, with source maps and release correlation decided at implementation time and
    the same payload exclusion list applied to error metadata.

13. **Session replay evaluation.** Only after privacy masking and a sampling strategy are
    designed. Not approved for capture; personal surfaces are never captured unmasked.

14. **Operational observability evaluation.** A Grafana Cloud or OpenTelemetry-compatible
    stack for Worker latency, error rates, provider failures, fallback behaviour, and
    quota health. Separate from product analytics and later than it; prefer a usable free
    tier with spending controls.

15. **Supabase accounts and sync**, when product scope reaches it. Auth, Postgres, and
    Storage per [ADR 0022](adr/0022-supabase-is-the-intended-backend-and-kuyara-is-not-local-first.md).
    Promoting existing device rows into an authenticated profile needs its own ADR. Until
    then the only obligation is the negative one: keep the boundaries migration-friendly
    and build no sync infrastructure.

Provider prices and quotas are deliberately absent from this list. Reverify them from
official sources when each item is implemented.

## Known Issues and Manual Verification Gaps

- Goal 7's validation pass (2026-09-09) recorded the following against the shipped screens; the table in Recently Completed carries severities, captures and follow-ups. At the largest accessibility text size, in both languages: the outfit detail's in-place captions overlap each other and the drawings (high); Weather's location card label breaks by character and the card grows past the viewport (high); Settings' native rows keep label and value on one line against ADR 0030 section 3 (medium); the AI status tinted row label does not scale (medium); the Closet's category chips clip at their fixed 40 point height (medium); the detail's emphasis pill leaves the screen (low); Profile's Closet heading, the preference pickers' header titles, the wardrobe form's colour options and the onboarding titles break by character in Turkish (low). Independent of text size: the SwiftUI birth date picker follows the device locale rather than the app language and shows today's date while unset (medium); `Screen`'s bottom inset double-counts the tab bar, leaving 110 points where 12 were intended (medium); Today's two alternate boards have different heights so their captions misalign (low).
- Today has no accessible refresh control since the header buttons left on 2026-09-08: the hierarchy shows pull-to-refresh only, so a screen-reader user refreshes through Weather's button. Whether Today gets a refresh action again is a design decision (goal 7 follow-up 7).
- `components/ui/stretchy-header.tsx` is built and tested, has no shipped caller since Today's board landed, and has no Reduced Motion gate; if it is wired in again it needs one, otherwise it is dead code to remove.
- The recommendation detail captions mark a garment only when the Closet records it as `owned` or `wanted`. An untracked garment, the default, draws no marker and no word so the two tracked states keep their weight, while its accessibility label still speaks the state ("Not in your Closet" / "Gardırobunda yok"); decided 2026-09-09 as a quiet-default rule rather than a third glyph. The screen also still carries the pre-existing per-garment owned/wanted button list below the board, because the control that changes ownership is deliberately undesigned in [ADR 0026](adr/0026-the-recommendation-detail-surface.md); it is the heaviest element on the screen and is the first candidate for that design.
- The native location picker marks the selected result with a localized trailing "Selected" value rather than a native selected trait, because `NativeListRow` exposes a trailing value only. Goal 7's pass (2026-09-09) could not run real VoiceOver or Accessibility Inspector; the XCUITest hierarchy shows every native row read as one element joining label and value ("Dil, Türkçe"), so a selected place reads as its name followed by "Selected". How VoiceOver itself speaks it stays with the real-device pass, the ninth goal 7 follow-up. Android rendering of the `@expo/ui` text field and the picker list is unverified, consistent with the repository's Android posture.
- App Store submission has unmet non-code prerequisites: a privacy policy URL, a support URL, the App Store Connect data-collection questionnaire, screenshots, and description copy. TestFlight distribution is unaffected. Since 2026-09-04 the first two of those also depend on the analytics decision: the privacy policy and the questionnaire have to describe analytics collection, and whether a consent or revocation mechanism is required is unresolved. See milestones 9 and 11 above.
- WeatherKit is live and serving, but only its success path is verified. Apple still does not document which HTTP status a request gets once the monthly allowance is exhausted, so the quota mapping remains an assumption; it lands on a fallback-eligible error either way, and the worst case is a demotion to Open-Meteo. The daily cap has never been reached, so `createDailyCappedWeatherProvider`'s `quota` branch is untested against Apple in production.
- The provider chain hides a broken provider. A failing adapter returns HTTP 200 from a lower-ranked source, so a green test suite and a successful deployment together do not prove the intended provider ran. Confirm `origin.sourceId` in a live response after any provider change.
- The Closet's native segmented control is untinted on iOS: the installed `@expo/ui` community segmented control reads `tintColor` only on Android, so ADR 0029 section 2's `brandPrimary` tint is Android-only until the package exposes an iOS tint. Recorded by the Closet lane on 2026-09-07.
- `outfit-composition.ts`'s `composeOutfits` has no production caller since 2026-09-08: `recommendOutfits` composes through `composeOutfitOptions`, which reuses `collectValidOutfits` and `selectDiverseOutfits`. Seven tests still exercise `composeOutfits`; fold them onto `composeOutfitOptions` and delete the function when the composition changes next.
- Android rendering of the native tab bar is unverified. Native Tabs renders Material tabs there, which is closer to Material 3 Expressive than the deleted hand-built bar was, but only iOS was checked. This is consistent with the repo's existing Android posture and was explicitly out of Milestone B's scope.
- `pnpm e2e:ios` passes 2/2 as of 2026-09-03, verified on iPhone 17 Pro / iOS 26.3. The suite had not run since the three-tab navigation change and needed three corrections, none of which was the failure that was anticipated. Maestro does resolve test ids on native tab bar items, so the open `UITabBar` question is closed. Note that Maestro drives an already-installed app and neither builds nor bundles: with an Expo dev client installed, Metro must be serving on `localhost:8081` first, or every flow fails at its first assertion against the red script-URL error screen.
- Outfit detail can show one accent-filled selected button per garment slot, so a four-piece outfit puts four filled elements in one viewport, in tension with [`design-language.md`](design/design-language.md) Law 1's one-accent-fill-per-viewport rule. The filled state matches the existing wardrobe owned/wanted filter convention and reads as a selection state rather than four competing calls to action, so it was left as is rather than inventing a treatment; whether Law 1 should carve out repeated selection controls is undecided. Goal 7 added (2026-09-09) that at the largest text size the Turkish pair breaks mid-word ("Bend e var", "İstiyo rum") while English wraps at the word; ADR 0026 decision 5's ownership control absorbs both points.
- In the dark theme `backgroundElevated` and `surface` resolve to the same value, and the dark elevation shadows are deliberately faint because the dark surface step already carries the separation. An elevated card therefore separates from the ground but not from a plain surface. No screen depends on that distinction today, so no dark tint was added for it.
- The resolved Android manifest has not been checked after blocking fine-location permission through Expo configuration.
- Real VoiceOver focus order and spoken grouping remain unverified for the Weather screen's stat and forecast groups. Goal 7's pass (2026-09-09) verified the accessibility hierarchy instead: the current-conditions block, each stat and each hourly row are single labelled elements in source order, and the refresh and change-location controls carry their names and roles. Only a device pass can confirm the spoken result; that pass is the ninth goal 7 follow-up.
- N2's background execution cannot be verified on the iOS Simulator and will require a physical-device check.
- A generated local iOS project may need its own ignored `.xcode.env.local` Node path before native builds.
- The neuron cost per call has not been measured against a live Workers AI call; ADR 0007's figure is still an estimate, not a measurement.
- Android rendering of the icon set, the garment artwork, and the new picker screens is unverified; only iOS was checked.
- Real VoiceOver verification of the Profile location section, the outfit detail captions and ownership buttons, and the picker screens has not been done. Goal 7's pass (2026-09-09) verified the hierarchy: each detail caption is one element speaking item, slot and ownership state, the ownership buttons carry `selected`, the location row reads label and hint, and the pickers' options carry the radio role with `selected`. The spoken result stays with the ninth goal 7 follow-up.
- In the dark appearance `brandPrimary`, `brandAccent`, and therefore every filled button and selected chip resolve to the same Quiet Sky, so the wardrobe list can show three filled elements at once against Law 1's one-accent-fill-per-viewport rule. This is a palette property of the dark theme rather than anything Milestone A changed, and fixing it would mean giving filled buttons a distinct dark role.
- [ADR 0010](adr/0010-status-colours-destructive-variant-and-defined-borders.md) states the destructive button's light label as `#FFFFFF` measuring 7.53:1 on `dangerInk`, but the appearance's on-brand colour is Cloud White `#EFF4F3`. The implementation follows the rule's wording and uses `textOnBrand`, which measures about 7.1:1, still far above the 4.5:1 threshold. The ADR's illustrative hex is the part that is off, not the decision.
- Turkish register is mixed inside single namespaces. `wardrobe.emptyAction` "İlk parçanızı ekleyin" and `wardrobe.emptyBody` use the formal "siz" while `wardrobe.detailsCaption` "Dokunmazsan katalog değerleri kullanılır" uses the informal "sen"; `profile.wardrobeHint` "Sahip olduğunuz ve istediğiniz" sits beside `profile.locationChangeHint` "Konumunu değiştirebilmen için". The approved detail-screen pair (ADR 0026, "Sende var") is informal. A register decision for the whole app has not been taken; the goal 4 to 6 sheets write their new strings in the informal register and this entry records the inconsistency they sit next to.
- `settings-screen.tsx` renders the notifications control as a bare React Native `Switch` with no `trackColor`, so it is the only control on a kuyara-drawn card that shows the system's default green tint rather than the palette. It is not a boundary violation (the rule covers `expo-haptics` and `@expo/ui`), but it is a system-colour control that [ADR 0019](adr/0019-adopting-expo-ui-at-the-control-layer.md) did not account for, and the goal 6 design moves the whole screen to the native list where the trade is explicit and tinted.
- Decided 2026-09-07: the birth date replaces the birth year. ADR 0015 carries the amendment inline, and `architecture.md`, `product-decisions.md` and `AGENTS.md` were updated with it. The remaining wording that still says birth year is inside ADR 0015's original sections, kept for the record under the amendment note.
- [`design-system.md`](design/design-system.md)'s Elevation ladder narrates three allocations in sequence, M6, M6.1 and the 2026-09-04 supersession, so the current rule has to be read out of a changelog. Consolidating it is deliberately deferred until the Direction E tokens land, because the 1.2:1 assertion it describes is still in `theme.test.mjs` until then; fold the history into one current statement in the same change that replaces that assertion.

# Dependabot keeps the pnpm workspace and the workflow actions current.
# Expo SDK versions are deliberately out of scope: `expo install --check` owns them.
version: 2

updates:
  - package-ecosystem: "npm"
    directories:
      - "/"
      - "/apps/mobile"
      - "/apps/worker"
      - "/packages/contracts"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 3
    # Let a release settle before it is proposed.
    cooldown:
      default-days: 7
      semver-major-days: 14
    groups:
      development-dependencies:
        applies-to: version-updates
        dependency-type: "development"
      patch-and-minor:
        applies-to: version-updates
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    ignore:
      # The installed Expo SDK dictates these versions.
      - dependency-name: "expo"
      - dependency-name: "expo-*"
      - dependency-name: "@expo/*"
      - dependency-name: "react-native"
      - dependency-name: "react-native-*"
      - dependency-name: "@react-native/*"
      - dependency-name: "@react-native-community/*"
      - dependency-name: "babel-preset-expo"
      - dependency-name: "jest-expo"
      - dependency-name: "eslint-config-expo"
      # react and react-dom are pinned exactly by the SDK's bundledNativeModules;
      # the Expo Doctor gate rejects any other version, so no update type applies.
      - dependency-name: "react"
      - dependency-name: "react-dom"
      # The type package moves with the SDK on majors only.
      - dependency-name: "@types/react"
        update-types: ["version-update:semver-major"]
      # Toolchain majors are separate, documented migrations, not weekly bumps:
      # jest-expo 57 depends on jest 29 internals and @typescript-eslint/parser
      # bounds the TypeScript major.
      - dependency-name: "typescript"
        update-types: ["version-update:semver-major"]
      - dependency-name: "jest"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@types/jest"
        update-types: ["version-update:semver-major"]
      - dependency-name: "eslint"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"

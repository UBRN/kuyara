{
  "name": "@kuyara/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env=\"\"",
    "test": "node --experimental-strip-types --test \"src/**/*.test.mjs\"",
    "typecheck": "tsc --noEmit",
    "bundle": "wrangler deploy --dry-run --outdir dist --env=\"\""
  },
  "dependencies": {
    "@kuyara/contracts": "workspace:*",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "wrangler": "4.131.2"
  }
}

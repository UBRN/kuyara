{
  "name": "@kuyara/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "bundle": "wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "wrangler": "4.114.0"
  }
}
